# 贡献指南

欢迎来到 applypack。这个项目的代码大部分由 AI 执行者按 `TASKS.md` 的任务推进，
但有两样东西机器写不了：**真实世界的黑话**和**对产品的挑剔**。这两样正是贡献最有价值的地方。

## 只有一个模块对社区开放：`knowledge/`

这是一个隐私优先的项目，仓库里的多数代码**能接触用户数据形状**（schema、加密、
渲染）。为了不让每个贡献者都先过一遍隐私评审，我们把开放面收窄到一个模块：

> **`knowledge/` —— 术语表 / few-shot 范例 / JD 黑话词典。**
> 非用户数据、不接触用户数据，是唯一欢迎直接 PR 的模块（AGENTS.md §3）。

它计划包含三类内容：

| 内容 | 作用 | 现状 |
|---|---|---|
| **动词白名单** | 简历要点开头的强动词（「主导」「上线」「落地」…），润色闸门据此判「弱开头」 | 暂内联于 `packages/core/src/rewrite`，待迁移（见下） |
| **JD 黑话词典** | 「3.25」「HC」「转正名额」这类黑话 → 规范表述，供匹配与改写参考 | 待建 |
| **few-shot 范例** | 优质要点改写范例（原文 → 改写 + 理由），供改写层做上下文 | 待建 |

### 贡献规则

1. **条目必须可证伪地有用**：动词条目给一个「为什么算强动词」的最短理由；
   黑话条目给一个真实出现的场景（JD 原句或面试原话，脱敏后）。
2. **宁缺毋滥**：动词白名单的代价模型是「误伤可以改词，放水闸门失效」——
   往白名单里塞模糊词（「负责」「参与」）会被拒。
3. **不收任何真实用户数据**：范例必须是合成或彻底脱敏的。出现真实姓名、
   手机号、公司内部项目名的一律拒收。
4. **条目落到断言**：词条表有 `satisfies` 类型与查重断言（重复动词、
   中英混入都会在 CI 红灯）。加词条不改断言的 PR 会被要求补。

### 动词表迁移路径

动词表白名单目前内联在 `packages/core/src/rewrite`（TASKS T3.2 的取舍：
「一个空目录不构成让闸门晚一天生效的理由」）。`knowledge/` 建起后它会迁出，
迁移时保持：

- 纯数据、零依赖、零 DOM（`knowledge/` 的 tsconfig 与 `core` 同一条红线）；
- 词条带 `version`，改动走 `knowledgeVersion`（消费方可在验收里钉版本）；
- 迁移当日 core 的全部断言一条不改、全绿 —— 这是「没改坏」的证据。

想认领这件事？开 issue 说一声，把迁移方案先对齐再动手。

## 其他贡献

- **隐私验证**：按 [`docs/privacy-verification.md`](./docs/privacy-verification.md)
  走一遍验证流程，任何一条不符即为最高优先级 bug，直接开 issue。
- **适配器快照**：北森 / Moka / 大易之外的平台适配器，先开 issue 附目标平台的
  DOM 特征分析（公开页面即可，**不要贴带个人信息的申请页截图**）。
- **Bug 报告**：附上能复现的最小档案（合成数据，不要用真实简历）与完整报错。
  记住本项目的主张：报错详情要能直接指向成因，如果报错让你看不懂，那也是 bug。

## 作业约定（写给人类与 AI 执行者）

- 开工前读 [`AGENTS.md`](./AGENTS.md)：7 条红线、技术栈冻结、禁止引入清单。
- **断言先于实现**：第一份产出是测试，且断言必须能红（变异验证）。
- 一个会话 = 一个任务编号；契约文档（DESIGN / AGENTS / TASKS）不许静默推翻。
- 提交信息用中文动词开头描述交付物；别动 `pnpm-workspace.yaml` 的
  `nodeLinker: hoisted`（见 AGENTS.md 的环境陷阱记录）。

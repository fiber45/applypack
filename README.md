# 简历智能生成与网申自动填充平台

一个**没有服务端**的求职工具：上传一次个人档案，按目标 JD 自动筛选并润色出专业简历，网申时辅助填表。

数据全部留在你自己的浏览器里 —— 打开 DevTools 的 Network 面板，你可以自己验证没有任何请求携带你的简历内容。

> **当前状态：设计阶段。** 尚未有可运行的代码。目录下的三份文档已经收敛完成，第一条竖切（schema → 加密 → 编译 → 匹配 → 硬校验 → 渲染）待开工。

---

## 文档

| 文件 | 作用 |
|---|---|
| [`DESIGN.md`](./DESIGN.md) | **为什么这么做** —— 完整技术方案，含 7 条 ADR（每条都记录了被否决的方案与否决理由） |
| [`AGENTS.md`](./AGENTS.md) | **怎么做** —— 作业约定、7 条红线、技术栈冻结、命令、禁止引入清单。执行者（人或 AI）开工前必读 |
| [`TASKS.md`](./TASKS.md) | **做什么** —— 按里程碑拆分的任务，每条带可执行的验收标准；§0 规定了先打通哪条竖切 |
| [`KICKOFF.md`](./KICKOFF.md) | 交接话术模板 —— 怎么把任务派给一个 AI 编码 Agent |

## 三条设计原则

1. **零后端。** 多用户隔离不是被"实现"的，而是被"取消"的 —— 没有服务端就没有多租户，隐私从架构承诺变成可自证的事实。
2. **验收不交给模型。** 简历最致命的错误是编造。LLM 输出的每一个数字都必须在原文中找得到，否则拒绝重写。这一层是确定性代码。
3. **不代提交。** 本项目只做填表辅助和提交前提示，提交按钮永远由你自己点。

## 已知限制

- **忘记口令，数据就没了。** 这是真端到端加密的代价，无法通过重置密码找回 —— 请务必导出加密备份。我们不会（也无法）替你保存任何后门。
- 无跨设备同步：数据在各自浏览器里。
- 需要自备模型 API Key（BYOK）。

## 许可证

[MIT](./LICENSE) —— 欢迎自由使用、修改、分发。

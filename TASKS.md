# 任务清单（TASKS.md）

> 每个任务 = 一个 PR。验收标准必须是**可执行的断言**，不是形容词。
> 顺序原则：**先打通一条竖切，再横向补齐**（见 §0）。

---

## 0. 先做这条竖切，其余一律后置

```
T1.1 schema → T1.2 加密 → T2.1 编译 → T2.2 匹配 → T3.2 硬校验 → T4a 渲染导出
```

打通这条链路后，你同时得到三样东西：**一个能跑的最小产品**、**三处可演示的工程亮点**（B 级不出网的测试 / 可解释的筛选理由 / 数字溯源闸门）、**一段完整的面试叙事**。

反过来，先把 M1 全部做完再做 M2，会在很长时间里没有任何可展示的东西 —— 对一个作品集项目来说这是最贵的错误。

---

## M1 · 骨架、加密与密文库

### T1.1 schema 定义 ✅ 已完成
产出：`core/schema/`
- [x] JSON Resume 基础上扩展 `i18n: {zh, en}`、`customFields`、`schemaVersion` + 迁移函数
- [x] 每个字段标好 A / B 级，B 级带 `neverSendToLLM: true`
- [x] 序列化 → 反序列化深相等
- [x] 旧版数据经迁移函数可升级（含一条真实的历史版本样例）

> 46 条断言，`pnpm test` 与 `pnpm typecheck` 均通过。
> 清单之外另加了两处编译期守卫（`level-spec.guard.ts` / `no-runtime-deps.guard.ts`），
> 把「漏标字段会编译失败」「core 里不存在 document / window / process」从注释变成持续可验证的断言。

### T1.2 加密原语 ✅ 已完成
产出：`core/crypto/`
- [x] Argon2id(WASM) 派生 KEK → 封装随机 DEK → AEAD 加密记录
- [x] 参数（内存 / 迭代 / 并行度）写进代码注释与文档，并说明取值依据
- [x] 错误口令解密必须抛错，**不得返回部分数据**
- [x] 有已知测试向量

> 59 条断言，与 T1.1 合计 105 条；`pnpm test` 与 `pnpm typecheck` 均通过。
>
> **一处与原计划的偏离（已记入 DESIGN.md ADR-9）**：AEAD 由 AES-256-GCM 改为
> **XChaCha20-Poly1305**。原因是实测发现 libsodium.js 的 WASM 构建（含 sumo）
> 完全不包含 AES-256-GCM —— 不是配置问题。要用它只能改走 WebCrypto，
> 那会要求 `core` 放弃零全局依赖。附带收益：192-bit nonce 让「随机 nonce」天然安全。
>
> **测试向量的两个独立来源**：`argon2-cffi`（Argon2 官方 C 实现）与 `PyNaCl`
> （libsodium 官方 C 实现），由 `scripts/gen-vectors.py` 生成，**不经人手转录**。
> 另用 `@noble/ciphers`（独立纯 JS 实现）做双向互解，验证算法本身而非传参。
>
> 清单之外另加：KDF 参数硬上限（信封参数来自外部文件，按不可信输入校验）、
> 信封头部用 AAD 认证绑定（改一个字节就解不开）、`decryption_failed` 与
> `invalid_params` / `unsupported_version` / `malformed_envelope` 的错误码区分。

### T1.3 密文库 + 加密备份 ✅ 已完成
产出：`core/vault/`
- [x] 导出 `.vault` → 导入后全字段深相等
- [x] 负向断言：口令丢失时不存在任何恢复路径（这条要写成测试，不是写成注释）

> 57 条断言，与 T1.1 / T1.2 合计 162 条；`pnpm test` 与 `pnpm typecheck` 均通过。
>
> **「不存在恢复路径」拆成了四组可失败的断言**（`vault/recovery.test.ts`）——
> 一个否定命题没法直接断言，所以每组对应一种**具体的**、本来能成为恢复路径的东西：
> ① 弱口令与常见默认值（含大小写、少一字符、多一空格等近似变体）；
> ② 文件结构封闭性（`.vault` 的键集合恰好是那 6 个，没有地方能藏第三把钥匙）；
> ③ **API 面封闭性** —— 把 `Vault` 的静态与实例方法集合写成断言。将来谁想加
> `recover()` / `reset()`，必须先改这一行，而那一行会出现在 diff 里；
> ④ 换口令的方向性（它需要**当前**口令，是向前的路径，不是向后的恢复路径）。
>
> **两处与原计划的偏离：**
>
> 1. **不做恢复码，只做换口令**（`Vault.changePassphrase`）。`DESIGN.md` 3.2 / §5 里的
>    「恢复码」与 §7 第 5 句「忘记口令数据就没了」原本直接冲突，删掉的是恢复码 ——
>    理由与代价认领见 `DESIGN.md` 3.2。
> 2. **修正了 T1.2 的 AAD 作用域**（记入 ADR-10）。payload 的 AAD 原本包含盐，
>    于是「换口令 ⇒ 换盐 ⇒ 内容永久解不开」，两层密钥设计会当场失效。
>    现在 payload 的 AAD 只绑定 `format` / `version` / `cipher.algorithm`。
>    信封的**字段与版本号都没变**（`version` 仍为 1），因此不涉及迁移 ——
>    T1.2 的全部断言一条没改就通过了。
>
> **两个新增的行为契约**，都由断言钉住：
> - `Vault.open` 成功 **⇒** 内容完好可读。解锁时会多解一次 payload 做完整性校验 ——
>   宁可多解一次，也不让「口令对」与「数据在」变成两条会分别失败的消息。
> - 锁定后读写抛 `vault_locked` 而非 `decryption_failed`：把「忘了先解锁」
>   报成「口令错误」，会把用户引向完全相反的处置方向。
>
> 清单之外另加：`VaultStore` 存储抽象（core 对介质零假设 —— 有一条断言用一个
> 「把文本存反」的 store 来证明）、`.vault` 文件名白名单、以及
> `deriveKek` 对非字符串口令的类型防御（此前它会逃出本层的错误契约，
> 抛一个普通 `TypeError` 而不是 `CryptoError`）。

### T1.4 B 级不出网测试 ✅ 已完成
产出：`core/egress/` + `core/egress/b-level-never-egress.test.ts`
- [x] mock LLM client，喂一份填满全部 B 级字段的档案，深度扫描捕获的 payload，断言零命中
- [x] CI 阻断

> 42 条断言，与 T1.1–T1.3 合计 204 条；`pnpm test` 与 `pnpm typecheck` 均通过。
>
> **落点从 `evals/` 改到 `core/egress/`。** `packages/evals/` 要到 T3.4（评测集接入 CI）
> 才建，而这条断言测的是 core 自己的组装层，不依赖评测框架。放 core 里意味着它
> 从第一天就跟着 `pnpm test` 跑，而不是等到评测集搭好才开始守门。
>
> **本任务真正的工作量不在测试，在于把「组装层」从零建出来。**
> AGENTS.md §6 的三层防空里，第 2 层原先只是文档上的一句话：
>
> - `project.ts` —— A 级投影。刻意用**白名单**（只保留有 A 级策略的子树）而不是
>   黑名单剔除。差别在失败模式：黑名单漏一个字段名 ⇒ 静默出网且无症状；
>   白名单遇到未声明路径 ⇒ 直接丢弃。两者都错了的前提下，后者是唯一可接受的失败方向。
> - `scan.ts` —— 深度扫描，两条独立通道：**路径扫描**（结构性，不依赖值长短）与
>   **值扫描**（覆盖逐字与分隔符变形）。
> - `request.ts` —— 出网路径收敛成 `assembleLLMRequest` 与 `callLLM` 两个函数。
>   `LLMRequest` 被拆成 `cachedPrefix` / `volatile` 两段，于是 DESIGN 8.1 的
>   「缓存断点必须打在档案之后」从一句会被忘记的劝告变成了**类型约束**：
>   调用方在类型上就无法把易变内容塞进前缀。
>
> **值扫描有一个必须写进 API 而不是藏起来的分界线。** 判据是「强标识符」：
> 含数字、含 `@`、或含汉字 ⇒ 阻断；纯 ASCII 字母词（`GitHub`、用户名、英文姓名）⇒
> 只进 `advisory`。依据是**误报代价不对称** —— 真标识符都带结构性字符，
> 而 `linzhiyuan` 在 A 级正文里出现的最常见原因是它在项目 URL 里。
> 把它当阻断，第一次跑 CI 就会红，而修法只能是把断言注释掉，那比不写还糟。
>
> **代价认领：纯 ASCII 短标识符的防线只有路径扫描 + 投影，值扫描抓不住它们。**
> 这条局限没有被藏起来 —— `b-level-never-egress.test.ts` 里有一条断言专门把它写明。
>
> 测试本身带**反向验证**（两条）：把原始档案直接塞进载荷、把 B 级值缝进 A 级文本，
> 断言必须失败。没有这两条，「零命中」无法与「测试根本没在跑」区分。
> 导出面也被写成断言 —— 想加 `sendRaw()` 之类的新通道，必须先改那一行。
>
> **顺带修了 T1.1 遗留的一处漏报**：`policy.ts` 的 `getAtPath` 用 `isPlainObject`
> 判断可访问性，而 `listLeafPaths` 会给数组产出 `profiles.0.network` 这样的下标路径。
> 结果是**任何穿过数组的路径在第一步就中断、静默返回 `undefined`** ——
> 整棵 `basics.profiles` 的 B 级值都不会被扫描器看到，且没有任何症状。
> 这正是 T1.4 存在的意义：不写这个测试，这个洞会一直留到有人真的去审代码。

### T1.5 扩展侧密文副本 ✅ 已完成
产出：`packages/extension/src/vault/`
- [x] 扩展持同一份密文，用同一口令独立解锁，会话内 15min TTL
- [x] 扩展重启后必须重新解锁
- [x] Web 端更新档案后，扩展能收到密文推送并刷新

> 46 条用例（3 个文件：`session` / `mirror` / `security`），core 侧新增 7 条
> （`loadPayload` 那组 + 一处 API 面断言），core 合计 **448**；
> `pnpm test`、`pnpm typecheck` 均通过。
>
> 落点是 `packages/extension/src/vault/`（AGENTS.md §3 目录表里的 `extension/`）。
> **WXT 与 manifest 仍未建**（T5.1）：本任务交付的是纯逻辑层，两个平台接缝
> —— `VaultStore`（来自 core）与 `CiphertextTransport` —— 各自留到扩展入口去实现。
> 本包不碰任何 `chrome.*` 全局，因此整层逻辑在 Node 里可确定性地跑。
>
> ---
>
> **① 「15min TTL」兑现成两条绳子，而不是一条。**
>
> | 绳子 | 默认 | 管的是 |
> |---|---|---|
> | 空闲 TTL | 15 min 无使用 | 用户走开之后暴露窗口自然关闭 |
> | 绝对上限 | 30 min（自解锁起算） | 「一直有人在用它」不能无限延长会话 |
>
> 只有空闲 TTL 的话，一个持有已解锁扩展的上下文只要每 14 分钟碰一次，
> 就能让密钥永远留在内存里 —— 那时 15 分钟不再是**上界**，只是一句描述。
> 上限取 2× 而不是更大：它的作用是封顶，不是照顾长表单；真需要连续填写时，
> 代价是一次口令输入，换来的是上界从「无限」降到 30 分钟。
> 选择「空闲」而不是「从解锁起算的绝对 15 分钟」，是因为真实填充是断断续续的，
> 中途锁在最难看的时刻（表单填到一半）会让人开始把口令写在便签上 ——
> **那才是真正的安全损失**。这个偏离与代价一并记在这里，而不是藏在代码里。
>
> **② 过期是惰性判定，零定时器 —— 而且这一点由 tsconfig 强制。**
>
> MV3 的 service worker 约 30 秒空闲即被终止，`setTimeout` 挂着的那一刻进程
> 可能已经不存在了。于是「用定时器把密钥清掉」是一个**会静默失效**的实现：
> 本地怎么测都对，装进浏览器就变成「密钥一直留在内存里」。这里的过期语义是
> 「有人来问时读一次时间」，`tsc` 的 `lib: ["ES2023"]` + `types: []` 让
> `setTimeout` 根本不存在 —— 那条错误路线不是被禁止，而是不可写（同红线 1 / 6）。
>
> **③ 同源判定是一次解密，不是比较头部字段 —— 为此 core 只多了一个方法。**
>
> `Vault.loadPayload(payload)`：用会话手里的 DEK 解开**指定**的 payload。
> 为什么不比较盐 / `wrappedKey`：一次正常保存不动头部，**而换口令会同时换掉这两样**，
> 于是「头部不同」在『改了口令』与『是另一个库』两种情形里都成立，
> 而这两件事的正确处置恰好相反（前者的内容我们读得懂、后者根本不该采纳）。
> 解一次是判决性的：解得开 ⇒ 同一把密钥 ⇒ 同源。
> 它不是后门：`#requireDek` 守在前面，锁定态抛 `vault_locked`
> （`recovery.test.ts` ③ 组的断言已同步加进 `loadPayload`）。
> 顺带把 `loadArchive` 重写成「对自己那份 payload 调它」—— 两条路径因此不会各自漂移。
> 代价：每次推送多解一次内容（推送频率是「用户点一次保存」，不需要优化）。
>
> **④ 一个被测试抓住的静默失效（本任务最值得记的东西）。**
>
> 第一版 `loadArchive()` 直接调 `vault.loadArchive()`，而 `Vault` 持有的是
> **解锁时刻的信封**。于是：推送被接受、存储被更新、`status` 一切正常、
> 同源判定也通过 —— 而用户读到的还是旧档案。没有任何报错，症状是
> 「刷新好像没生效」。修法是把两件事分开：**`Vault` 只做密钥持有者，
> 内容的真相在会话的 `#ciphertext` 里**（读档案时用镜像里**此刻**的 payload）。
> 这正是 AGENTS.md §7 说的那件事：看起来正确的实现是错的，读代码看不出来。
>
> ---
>
> **三条边界与已知局限（写下来而不是绕过去）：**
>
> - **锁定态判不了同源。** 手里没有密钥，除了「它看起来是个信封」以外没有可验的
>   东西。所以锁定态只做结构校验就采纳，但把「密钥封装代际变了」作为线索交给调用方，
>   界面因此可以说出「若提示口令错误，先确认 Web 端是不是改过口令」，
>   而不是让用户以为是自己记错了。代价如实展示在 `mirror.test.ts` 里：
>   外来的密文会在 `unlock` 时以 `decryption_failed` 暴露（与「口令错」在数学上不可区分）。
> - **来源认证不在本模块。** 推送方是另一个 origin（Web 端），本模块拿不到它的身份；
>   第一道门是 manifest 的 `externally_connectable` 白名单。`transport.ts` 里写明了这一点。
> - **换口令后的推送会主动降级为锁定**（`lock_reason: key_changed`）：内容读得懂
>   （DEK 没换），但新的 `wrappedKey` 能不能被新口令解开，手里没有口令，验不了。
>   继续拿一份「解锁路径无法验证」的副本干活，会在某次重启后炸在最糟的时刻。
>
> **清单之外另加（都有断言）：**
> - **推送不续期**：外部写入不是「使用」。否则一个能写存储 / 能发消息的页面
>   只要定期推一次空更新，就能让密钥永远留在内存里。
> - **幂等快路径必须在排队之前**。写存储会触发一次变更回声，回声再进队列就会形成
>   「写入等回声、回声等队列、队列等写入」的环 —— 同步回声直接死锁。
>   有一条用**同步回声**的存储做的断言同时验证「不级联」与「不死锁」。
> - **推送串行化**：并发两次推送不交错，最终状态一定是后到的那一份
>   （用一个「第一次写入更慢」的存储把这条断言变成真的能失败的断言）。
> - **密钥与明文零落盘**：本模块唯一的落盘出口是注入的 `VaultStore`，
>   测试把每一次写入的原文都记下来并扫描（含反向验证：那些明文确实在档案里、
>   推一份明文会被拒且不会写进存储）。
> - **坏配置拦截**：超时参数要求有限正数 —— `NaN` 让所有比较为 false（**永不过期**），
>   `Infinity` 是「永不过期」的另一种写法，两者都会「看起来在工作」。

---

## M2 · 编译与匹配

### T2.1 编译层 ✅ 已完成
产出：`core/compile/`（附 `core/verify/` 的溯源原子操作）
- [x] LLM 把 JD 与经历编译为结构化字段，强制 strict schema
- [x] 坏 JSON 率 = 0（strict mode + 有限重试）
- [x] schema 快照测试

> 64 条断言，与 T1.1–T1.4 合计 268 条；`pnpm test` 与 `pnpm typecheck` 均通过。
>
> **「坏 JSON 率 = 0」的实现方式是把它变成二值契约。** 不是让模型每次都对，
> 而是让 `compileStructured` **要么返回通过 schema 的对象，要么抛 `CompileError`** ——
> 没有第三种返回。坏 JSON 真正伤人的地方从来不是那次失败，而是它变成半成品
> 继续往下走：下游某个字段莫名其妙是 undefined，而没人知道源头在哪。
> 断言覆盖：一组坏样本（空回复、无 JSON、语法错、类型错、多字段、裸数组）
> 逐个验证必然抛错。
>
> **prompt 里的 schema 形状是从 schema 生成的，且有断言钉住同步。**
> 手写两遍必然漂移：改了 schema 忘了改 prompt，症状会表现为「模型能力不行」，
> 实际是两份文档不同步。`schema-snapshot.test.ts` 里那条
> `expect(JD_SYSTEM_PROMPT).toContain(shape)` 让这件事在结构上不可能。
>
> **两道闸门，第二道也接进重试。** schema 保证形状，保证不了含义 ——
> `numbers` 是合法字符串数组，但它可以是模型编的。所以 runner 支持
> `validate` 回调，语义失败**也是重试理由**：一个能被自动纠正的错误，
> 没有理由变成一次崩溃。经历编译挂了两条确定性检查：
> ① `sourceText` 必须是输入原文的片段（它一旦被润色，下游的幻觉校验就
> 失去参照物，而且是静默失效）；② `numbers` 必须真实出现在该条的 `sourceText` 中。
>
> **`archive` 改成可选。** JD 编译的上下文里根本不该有用户数据 ——
> 传一份空档案进去，只会在载荷里留下一个 `{}` 让人误以为「档案已就位」。
>
> 两条断言推翻了初稿的实现与我的假设，都记在对应测试的注释里：
> - `extractJsonCandidate` 原本**先**做「第一个 `{` 到最后一个 `}`」的切片。
>   于是模型返回 `[{...}]` 时外层数组被切掉、里面那个对象顺利通过校验 ——
>   **一次形状错误被伪装成一次成功**。改成「整体是合法 JSON 就原样返回」之后，
>   数组会如实落到 schema 上被拒。
> - 初稿写着「数字被简化不算幻觉」，于是放行 `4.2 → 4`。这是错的：
>   4.2 和 4 是两个数，而「仅有的数字也都对得上」正是这条闸门要保住的东西。
>   归一化的正确边界是**同一数值的不同写法**（`4.0` ≡ `4`），不是「看起来差不多」。
>
> 本任务另建了 `core/verify/` 的两个原子操作（数字抽取、文本溯源）。
> 提前放进最终归属地，是为了避免 T3.2 时搬家 —— 那里会在此之上补
> 关键词覆盖、长度、动词重复率等指标。

### T2.2 匹配引擎 ✅ 已完成
产出：`core/match/`
- [x] 加权规则 + 理由生成，**全程零 LLM 调用**
- [x] 同输入同输出（确定性）
- [x] 每条经历（含被淘汰的）都带可读理由：命中关键词 + 分数构成
- [x] 单测必须覆盖「为什么删了我这段实习」这个问题能被自动回答

> 31 条断言，与前面合计 299 条；`pnpm test` 与 `pnpm typecheck` 均通过。
>
> **这一条原文写的是「embedding + 加权规则」，已删掉 embedding 三个字。**
> 它与 `AGENTS.md` §2「明确不允许引入：向量数据库」和 `ADR-5`「不对用户数据做 RAG」
> 直接冲突 —— 是本文档早于那两条决策写下的残留。留着它的后果很具体：
> 下一个执行者会照着清单去装一个 embedding 依赖，而那条 ADR 的否决理由
> （数据量小、负收益、召回不稳）从来没被推翻过。**清单与 ADR 冲突时，改清单。**
>
> **本层最重要的设计是 `MatchItem` 的形状。** 匹配引擎接受的不是 `ArchiveV1`，
> 而是一个只含标题、机构、时间与文本片段的窄结构。这不是为了解耦好看 ——
> 它让「匹配层读不到 B 级字段」**在类型上成立**：传 `ArchiveV1` 进来，
> 写一行 `archive.basics.contact.phone` 就能读到；传 `MatchItem[]` 进来，
> 那个字段根本不存在。红线 1 只有变成形状约束才可能长期有效。
>
> **理由的 `impact` 采用「相对满分的差额」语义**（命中项 = 0，失分项 = 负值），
> 由此得到一条**可核对的恒等式**：`Σ 全部 impact ≈ 总分 - 100`。
> 用户能把报告上每条理由的分数加起来，验证它确实等于自己离满分差了多少 ——
> 这是他能验证「这套打分有没有在糊弄我」的唯一手段，也正是 ADR-4
> 选择确定性匹配时承诺的东西。容差来自「低于 0.5 分的项不单独成条」。
>
> **「零 LLM 调用」是可断言的**，不只是代码里没写：`matchItems` 返回普通对象
> 而非 Promise（真出网必然异步），导出面里不存在任何 client / llm / egress 名字。
> 两条断言守着这一条。
>
> 一处文档与实现不一致，保留的是实现那一边：`cutoff` 初稿注释写「全部入选时为 0」，
> 实际返回最后一名入选者的分数。若让它随「有没有淘汰」改变含义，
> 读的人就得先判断场景才能解释这个数。现在它是稳定的一句：**入选者中的最低分**。

### T2.3 理由展示 UI ✅ 已完成
产出：`packages/web/src/features/match-report/`（+ `core/match/report.ts`）
- [x] 逐条可展开查看分数构成
- [x] 淘汰项与选中项同等可查

> 32 条断言（core 28 + web 18，其中 web 那 18 条里有 11 条是本次新增的
> 「同名断言按包拆开」）。core 658 / 34 文件 · extension 46 · evals 11 · web 18；
> `turbo typecheck test build` 四包全绿；`vite build` 产物在 `packages/web/dist/`。
>
> **`packages/web` 是本任务顺带建起来的**（Vite + React + Tailwind 4 + jsdom）。
> 它此前只是 DESIGN 3.1 里的一行目录 —— T4a 的 PDF 部分和 M5 的填表都卡在它上面，
> 所以建包这件事躲不过去。**但建包本身不算交付**：本任务在它里面落了一个真特性，
> 不是一个空骨架（Demo 外壳只有匹配报告这一页，其余特性随各自任务建）。
>
> **本任务的形状与 T4d 一样：把不变量做成形状约束，而不是写成注释。**
> 两条验收各对应一处：
>
> | 验收 | 落法 |
> |---|---|
> | 淘汰项与选中项同等可查 | `MatchReportRow` 是**判别联合**：`status: 'rejected'` 的行上 `rejection` 非空。渲染层因此**写不出** `if (row.rejection === null) return null` 这种「静默少一块」的分支 |
> | 逐条可展开查看分数构成 | `composition` 恒为四项，**得 0 分的维度也在** |
>
> **第二处的反面才是这条验收真正的敌人。** 最自然的实现是学 `reasons` 那样
> 只列有失分的维度 —— 更干净、代码更短，后果是一个近乎满分的入选项展开后只剩一行、
> 被淘汰的展开后有四行，用户会以为界面只帮他检查了一项。而它的症状是**没有症状**：
> 两边都渲染成功、都不报错、也没有测试会红。所以 `composition` 恒四项，
> 并且有一条断言专门构造 `highlightCount === 0` 的条目把「0 分维度」逼出来。
>
> **顺手修掉一处「文档与实现漂移」（与 T2.2 那次同类）。** `core/match` 的模块头
> 一直写着「`report.scored` 含全部条目，淘汰项额外带一份 `explanation`」，
> 而实现把解释放在**顶层平行数组** `MatchReport.explanations` 里，
> 消费方（`agent/loop.ts`、`agent/tools.ts`）得自己按 `entryId` 再 join 一次。
> 漏 join 的表现是「淘汰行看起来信息更少」—— 可读、不报错、无测试覆盖。
> 现在解释挂在 `ScoredItem.rejection` 上，`explanations` 保留为**由它投影出来**的
> 便利数组，并有一条断言钉住两者恒等。**保留的是文档那一边。**
>
> **新增 `MatchReport.weights`。** 分数构成是 `raw × weight × 100`，不把本次实际
> 用的权重随报告返回，消费方就只能猜「用户传了自定义权重没有」，猜错的表现是
> 四行数字加起来**恰好差一点**，看起来像四舍五入误差。有断言用非默认权重验这件事。
>
> **`Σ contribution === total` 是精确成立的，不是约等于 —— 靠最大余数法。**
> 四个各差 0.05 的数相加，屏幕上会出现「8.3 + 4.5 + 2.2 + 2.3 = 17.3，总分却是 17.4」，
> 用户的结论不会是「舍入误差」而是「这分数是编的」—— 而 T2.2 承诺的恰恰是
> 「用户能把报告上的数加起来核对」。代价是单项可能被挪动最多一个刻度（0.1 分），
> 这个代价由一条断言封顶，防止这套算法从「让显示自洽」滑向「替用户调分」。
> **得 0 分的维度不参与凑数**，恒显示 0。
>
> ## 变异测试（7 个变异，全部被抓住）
>
> | 变异 | 抓住它的断言 |
> |---|---|
> | 淘汰行不渲染「分数构成」 | 6 条红（含两条对称性） |
> | 构成过滤掉贡献为 0 的维度 | 5 条红 |
> | 明细恒展开（折叠失效） | 3 条红 |
> | 差值用无符号格式化 | 1 条红 |
> | **界面自己再舍入一次（`toFixed(2)`）** | **1 条红 —— 这条断言是补写的** |
> | 入选行不给「关键项」 | 1 条红 |
> | core 改回朴素四舍五入 | 2 条红 |
>
> **第五条一开始没被抓住，这就是变异测试的价值所在。** 原本那条断言比的是
> `Number(屏幕上的文本)`，而 `Number('4.10') === 4.1` —— 二次舍入在数值上不可见。
> 改成逐格比**文本**之后才红。教训写进技能库：**「比数字」会放过一切格式漂移，
> 而格式漂移的代价是项目里出现第二套数字口径。**
>
> ## 刻意的缺口
>
> - **`web/features/` 下只有 `match-report` 一个目录。** 档案编辑、生成、预览、
>   导出属 M5 / M6，各自的任务里建。
> - **没有引入 `@testing-library/jest-dom`，也不开 vitest globals**（`afterEach`
>   显式注册）。少一个依赖，也少一处「看起来在跑其实没跑」。
> - **Demo 样本不走 `core` 的 `__fixtures__`。** 验收样本的取值是为了钉断言，
>   引进产物会形成反向依赖：改验收样本会改动 Demo 展示，而维护 Demo 的人不知道
>   自己在动测试。代价是两份数据，认领。
> - **网络不请求这件事没有写成 jsdom 断言。** 「`fetch` 没被调用」在一条从不发请求
>   的链路上是恒真的，写下来只会变成一条永远绿的假证据。它由三处结构性事实承担：
>   `core` 的 `lib` 不含 DOM 且零运行时依赖、`match` 的导出面无出网名字、
>   `index.html` 的 CSP 写着 `connect-src 'none'`。M6.4 的自证步骤从这里开始成立。

---

## M3 · 改写与闸门（本项目的技术亮点区）

### T3.1 逐条并发改写 ✅ 已完成
产出：`core/rewrite/`
- [x] 上下文顺序：系统提示 → few-shot → 用户档案 →〔缓存断点〕→ JD → 本轮任务
- [x] 每条的裁剪必须**保留未经改写的原文**
- [x] 40 条并发：总延迟 ≈ 最慢一条；单条失败只重试该条
- [x] 风格锚点：已定稿前 3 条带入后续调用

> 39 条断言，与前面合计 388 条；`pnpm test` 与 `pnpm typecheck` 均通过。
>
> **清单的第 3 条与第 4 条互相拉扯，这是本任务唯一真正的设计问题。**
> 40 条同时发出时，它们**开始**的那一刻不存在任何「已定稿」的条目 ——
> 第 4 条不可能成立；反过来严格先串行跑出 3 条锚点，第 3 条的延迟承诺就废了。
>
> 处理办法不是取折中，而是把「锚点从哪来」分成两种，各自把承诺兑现干净：
>
> | 场景 | 锚点来源 | 分批 | 延迟 |
> |---|---|---|---|
> | 常规（默认） | `options.anchors`，来自**上一轮**已定稿的条目 | 否 | ≈ 最慢一条 |
> | 首轮就要锚点（`bootstrapAnchors`） | 本轮前 `anchorCount` 条 | 是 | ≈ 两段之和 |
>
> 默认走第一种，因为 T3.3 的 Agent 循环本来就有多轮：第二轮天然带着上一轮的定稿，
> 于是「全量并发」与「有风格锚点」同时成立，一分钱不用多付。
> 第二种只在「必须一次成型」时打开，**代价写在参数说明里**，不是让调用方事后自己撞上。
>
> **`concurrencyPeak` 被记进报告，是为了让并发度从一句话变成一条断言。**
> 「总延迟 ≈ 最慢一条」这条性质本身很难测准（绝对毫秒数会随机器负载漂移），
> 但它等价于一个完全确定的量：**峰值在途数 == 候选条数**。
> 串行实现下峰值必然是 1，因为每条 `await` 之前前一条已经减到 0。
> 于是两条断言各自守着一边：峰值管「有没有真的并发」，墙钟上界
> （`elapsed < 串行基线的二分之一` + `elapsed >= 最慢一条`）管「并发有没有真的省时间」。
>
> **与 `parse.ts` / `runner.ts` 一处方向相反的选择：重试不整体回显模型原文。**
> 编译层的重试会把上一轮输出回显给模型 —— 它的失败是「结构缺字段」，
> 模型必须看到自己产出的那份结构才知道缺在哪。改写的失败天然是**「这一句」**：
> 闸门的 `detail` 里已经引用了那句话（`「40」在原始素材中找不到依据：…`），
> 再回显整份 JSON 是纯冗余。而冗余在这里不是无害的 ——
> 整份回显会把模型上一轮的全部自由发挥带进重试请求，而重试请求的输入
> 本该只有原文与 JD。8.5 主张「反馈粒度决定能否收敛」，那就该只带那个粒度。
> 断言：重试载荷里不出现 `"sourceSpan"` 与 `"keywordsHit"`，
> 且任务消息与首次**逐字相同**（原文不因重试被改写）。
>
> **重试提示只带 `fatal` / `hard`，把 `target` 层滤掉。** 一次重试的触发原因
> 必须唯一：如果提示里同时写着「数字 40 是编的」和「量化率只有 0%」，
> 模型去修量化率的最省事办法就是**再编一个数字** —— 而它本来就是被叫来修
> 「编数字」这个问题的。`target` 层是 T3.3 的目标函数，不是本层的重试理由。
>
> **`sourceSpan` 与 `evidence` 不是装饰。** 两者都会被本地逐字比对
> （`isGroundedIn`），转述、概括、润色过的片段一律判为不合规并触发重试 ——
> schema 里留一个不受约束的字段，比没有这个字段更糟：它会让读者以为系统在做事实溯源。
>
> **数字的溯源范围是「本条原文」，不是「整份档案」。** 把另一条原文里的数字
> 搬到这一条，一样判为幻觉 —— 在本条的语境里它就是编的。
> 这条断言顺带证明了失败隔离不只是「互不干扰」，也是「互不背书」。
>
> **`matchedKeywords` 由本地计算，模型自报的 `keywordsHit` 不参与任何判定。**
> 一个可以自报命中率的改写器，在没人核对的时候会把命中率报得很高。
> 这与 AGENTS.md 红线 6（验收器不许是 LLM）是同一个原则：验收标准不能由被验收者提供。
> 自报值仍保留在产物里，因为它对未来轮次的诊断有用 —— 只是它不算数。
>
> **清单之外另加：**
> - `RewriteCandidate` 上**只有一个文本字段** `sourceText`。这是 8.2
>   「绝不先总结再改写」的根治办法：如果同时提供 `summary` 与 `sourceText`，
>   第一个偷懒的调用方就会只填 summary（更短、更省 token、看起来信息密度更高），
>   而溯源闸门会在**没有参照物**的情况下继续报「通过」—— 静默失效，
>   症状是「闸门好像从不拦东西」。
> - 结果顺序 == 候选顺序（并发完成次序不参入），有一条用「让靠前的条目更慢」构造的断言。
> - `rewritten.length + failed.length === candidates.length` —— 没有任何一条会被静默丢掉。
> - 出网边界的回归断言：**同一批 40 次请求的缓存前缀逐字相同**（缓存能命中的前提），
>   以及 **JD 的哨兵串不得出现在缓存前缀里**（8.1 那句「常见错误」的正式回归）。
>   哨兵串是特意另造的一组（`数据平台研发工程师` / `Flink` / `构建实时数仓与指标平台`）——
>   满档案 fixture 里恰好也有「推荐系统」「PyTorch」，用它们做断言会在与顺序无关的
>   情况下意外通过，那是一条假断言。
>
> 一处与原清单的字面偏离见 `rewrite/schema.ts` 文件头：8.3 给的是
> `{ bullets: [...] }`，而 8.4 要求逐条调用 —— 外层那个恒为长度 1 的数组被删掉了，
> 8.3 想表达的四个字段一个不少。

### T3.2 硬校验闸门 ✅ 已完成
产出：`core/verify/`
- [x] 注入一条含虚构数字的 bullet → `pass: false`，`failures[].reason === "number_not_in_source"` 且带 `offending`
- [x] 关键词覆盖率、长度上限、动词重复率、量化率、强动词开头 —— 逐项有独立用例
- [x] 一票否决项（数字溯源）必须能被单独测

> 29 条断言，与前面合计 328 条；`pnpm test` 与 `pnpm typecheck` 均通过。
>
> DESIGN 8.6 那张指标表的七项全部落地，并分成**三层严重性**：
>
> | 层 | 指标 | 不达标的后果 |
> |---|---|---|
> | `fatal` | 数字溯源 | 无条件失败（一票否决） |
> | `hard` | 强动词开头 / 被动语态 / 长度 / 动词重复 | 失败 |
> | `target` | 量化率 / 关键词覆盖 | **不算失败**，但出现在失败列表里 |
>
> **`target` 不算失败是刻意的。** 闸门的用途是「有没有撒谎」，不是「写得好不好」——
> 若把量化率也做成阻断项，一份还没改好的草稿会连「检查有没有编数字」都过不去，
> 而 T3.3 的 Agent 循环恰恰需要先拿到一份**诚实的**稿子，再逐轮优化它的指标。
> 所以 `pass` 只看前两层，`failures` 里三层都有。有断言专门守着这一条。
>
> **「一票否决项能被单独测」的实现方式是让它能被单独调用**（`verifyFacts`）。
> 否则想验证数字闸门，就得先构造一份同时在动词、长度、量化率上全部达标的稿子 ——
> 而那样的测试某天因为别的原因变红时，你会以为是数字闸门坏了。
>
> 两条实现细节带了取舍：
> - **动词表用白名单**，因此必然误伤（「累计上线 12 个特征」会被判为弱开头，
>   因为首二字是「累计」）。这个代价是有意接受的：误报的修法是换个动词，
>   几秒钟；而为了让所有输入都通过去放宽词表，闸门就没了作用。
> - **中文按字数、英文按词数**分别设限（45 字 / 25 词）。用同一个数字会让
>   英文的 45 词放宽到中文的两倍信息量。
>
> 动词表最终应迁到 `knowledge/`（AGENTS.md §3 说的唯一对社区开放的模块），
> 现在内联在 core 是因为那个包还没建 —— 一个空目录不构成让闸门晚一天生效的理由。

### T3.3 有界 Agent 循环 ✅ 已完成
产出：`core/agent/`
- [x] 工具白名单：`score_match` / `keyword_gap` / `rewrite` / `verify_facts` / `ask_user`
- [x] 轮数 + 预算 + 超时三重硬终止
- [x] 验收器非 LLM（架构上无法绕过）
- [x] mock 一个「永远失败」的改写：循环在 N 轮内终止并返回失败，不死循环

> 45 条断言，与前面合计 433 条；`pnpm test` 与 `pnpm typecheck` 均通过。
>
> **本任务唯一真正的决策：控制流是确定性的，模型只出现在 `rewrite` 里。**
>
> DESIGN 9 把这一层写成「Agent + 白名单工具 + 确定性闸门」，读起来像
> 「模型自己决定调哪个工具」。**那条路线会让 DESIGN 9 自己的可测试性主张失效**：
>
> > 同一 `(profile, JD)` 的**通过与否**应高度一致（措辞可不同）。
>
> 若由模型挑工具、挑顺序、挑停不停，这句话就退化成对模型稳定性的祈愿，
> 而 T3.4 的评测集立刻失去意义。DESIGN 8.4 已经明确把「判断力」放在
> **改写**那一步 —— 那一步本就在模型手里。所以循环、优先级、终止判定全部是代码。
>
> 附带收益：AGENTS.md 红线 1（不得读 B 级字段）与红线 6（验收器不许是 LLM）
> 从「被禁止」变成「**无法发生**」。红线 6 的落地方式尤其值得记下来 ——
> `verify_facts` 的实现是 `Promise.resolve(verifyBullets(...))`，
> **同步算完再包一层 Promise**，因为一个出网调用不可能同步完成。
> 再叠上判定函数的签名 `summarizeVerdict(result: VerifyResult, …)` ——
> 参数里没有任何模型能影响的东西。想在判定里加入「模型的自我评价」，
> 必须先往 `VerifyResult` 里塞字段，而那个类型由不 import `egress` 的
> `core/verify` 产出。三个模块之间形成的是一个绕不过去的结构，不是一条纪律。
>
> **「不死循环」的真正保证是 `no_progress`，轮数上限只是兜底。**
> 靠上限终止意味着钱已经花完了；`no_progress` 是在看出无解的那一刻停：
> 连续 N 轮失败签名完全一致 ⇒ 输入一个字都没变 ⇒ 再调一次模型是纯浪费。
> 默认 `noProgressLimit = 1`（两次同样的失败就停），TASKS.md 那条勾要求的是
> 「N 轮内终止」——实测 `maxRounds = 6` 时**第 2 轮就停**，断言写的是
> `rounds === 2` 且 `rounds < 6`，而不是「≤ 上限」。
>
> **失败签名只看「哪一条、违反了哪条规则」，不看具体违规内容。**
> 这条取舍有断言守着：让 mock 每轮编一个**不同**的数字（40 → 41 → 42…），
> 若签名带上 `offending`，这一串「换个数再编一次」就会被算作进展，
> 于是白白多跑四轮。规则没被修好，换一个数字不该换来更多轮次；
> 想给模型多几次机会，该动的是 `noProgressLimit`。
>
> **三条绳子必须各测一条。** 合成一句「总之会停」的断言，会在只剩下最多余的
> 那条绳子还有效时依然通过 —— 而三条绳子的意义完全不同：
> 轮数用满 ⇒ 「应该会收敛」判断错了；预算用满 ⇒ 在收敛但太贵；
> 超时 ⇒ 卡住了，问题在环境或模型侧。**报错理由写错，用户会被引向错误的处置方向。**
> 所以 `checkRopes` 的优先级也被单测钉住：三条同时成立时报最容易被用户
> 行动化解的那一条（轮数与预算都是一行配置，超时通常意味着环境问题）。
>
> **一处必须写明的局限：超时是「轮与轮之间」的检查。**
> core 没法中断已经飞出去的请求 —— 那需要一个 `AbortSignal`，而它是宿主环境
> （`fetch`）的东西，属于 Web 端的 `LLMClient` 实现。所以本层的超时语义是
> 「发现已经超时就再也不发起新的一批」，一批请求本身的耗时上限由那个实现负责。
> 这不是遗漏，是分层边界；写清楚比含糊过去强。
>
> **零产出必须显式认输。** `verifyBullets` 在零条 bullet 上的 `pass` 是 `true`
> （没有条目就没有违规），于是「一条都没生成」会被报成「通过」。
> 所以 `verdict.pass` 要求 `blocking` 与 `unresolved` **同时**为空，
> 且目标条数为 0 / 候选全部落选时直接走 `needs_user`，不进循环。
>
> **`unfixable` 与 `failures` 是两件事，没有合并。**
> `failures` = 「产出了，但违反闸门」（有稿子，稿子不对）；
> `unfixable` = 「根本没产出」（连稿子都没有）。只报「3 条未通过」而不说
> 是哪一条、因为什么，用户拿到的就是一个无法行动的数字 ——
> 所以 `unfixable` 承载最后一次的具体失败对象，并直接生成追问文案。
>
> **一条断言是循环的前提，所以它被单独写出来：**
> 「阻断级失败的 `bulletId` 一律不是 `<全局>` 占位符」。
> 循环的「下一轮重做哪些」完全依赖这一条；反向断言同时检查
> **全局占位符确实存在、只是全是 `target` 层** —— 没有这一步，
> 前面那条断言在「闸门根本没产出全局失败」时也会通过。
>
> **预算的计量放在循环层，用包装 client 实现，没有碰改写层。**
> 改写层不需要知道「这次调用属于预算里的第几笔」，而预算本来就是循环的缰绳。
> 真实用量（provider 报的 `usage`）优先于估算，厂商不报就退回估算 ——
> 于是精度随实现变化，但绳子不会因为缺字段而失效。
> 顺带给 `LLMResponse` 加了一个**可选**的 `usage` 字段（T1.4 的断言一条没改）。
>
> **与 DESIGN 9 的一处措辞对齐：输入是 `maxBullets`，不是「目标页数」。**
> 页数 → 条数的换算取决于版式（字号、行距、栏数），属于渲染层（T4）。
> 在这里接受「页数」等于逼 core 猜一个版式假设，而那个假设在 T4 一定会变。
>
> **`verify_facts` 的语义比 DESIGN 9 的描述宽一点**：它返回完整的
> `VerifyResult`（三层严重性），而不是只返回 fatal 层。循环每轮要同时知道
> 两件事 ——「有没有撒谎」（继续不继续）与「离目标还差多少」（怎么向用户交代）。
> 分两次调用同一个纯函数没有意义，而 fatal 层是完整结果的子集，语义没有被削弱。
>
> **清单之外另加：**
> - `dispatchTool` 是唯一的工具入口，未知名字**抛错而不是返回空结果** ——
>   静默降级成「这个工具没产出」，在下游看起来只是「这轮没效果」。
> - 审计日志 `log: AgentStep[]` 让「用了哪些工具」可事后核对，
>   断言是「日志里出现过的工具名 ⊆ 白名单」且五类都真的被用过。
> - **通过 ≠ 完整**：`status: 'passed'` 的产物里仍可能带着追问 ——
>   JD 要的关键词在素材里一处都没有（`keyword_gap` 读**原文**而不是改写产物：
>   素材里没有 = 需要用户行动；素材里有但没用上 = 循环自己还能修，属于指标不属于追问）。
> - 端到端再测一次 B 级不出网：满档案 + 整个循环跑完，全部捕获请求零阻断命中，
>   并反向验证「档案里确实有 B 级值、A 级内容确实进了载荷」。

### T3.4 评测集接入 CI ✅ 已完成
产出：`packages/evals/`（用例 + 执行器 + CI 入口）与 `core/verify/parity.ts`
- [x] ≥ 30 条 JSON 用例，覆盖幻觉数字、跨语言不一致、超长、关键词缺失
- [x] CI 阻断，输出失败用例明细

> **37 条用例**（分布在 9 类场景上）+ 11 条元断言；core 侧新增 8 条
> （`verify/parity.test.ts`）。core 合计 **441** 条，`pnpm test`、
> `pnpm test:evals`、`pnpm typecheck` 均通过。
>
> **报告必须把「漏报 / 多报 / 数值不符」分开写。** 一句「用例 hallu-01 失败」
> 等于没说 —— 三种问题对应三种完全不同的处置：
>
> | 现象 | 性质 |
> |---|---|
> | 漏报（该拦的没拦） | **安全性**问题，最严重 |
> | 多报（拦了不该拦的） | **可用性**问题，用户会被挡住 |
> | 数值不符（方向对、细节错） | **可解释性**问题：理由指错了地方 |
>
> 合成一个布尔值之后，一次多报与一次漏报看起来一模一样 ——
> 而那正是评测集最该替人分清楚的事。三个 `it()` 专门测这三种报告能被区分。
>
> **期望写成「失败集合恰好等于这些」，而不是「至少包含」。** 后者会让一个
> 新增的误报悄悄混进来。这条之所以敢写这么严，是因为闸门是纯函数
> （AGENTS.md 红线 6）—— 同一条用例的结果逐字可复现。
>
> **`clean` 这一类必须存在。** 没有它，用例集可以靠「把所有输入都判失败」
> 刷成全绿 —— 这正是 T1.4 那次「反向验证」留下的教训：**无法与「测试根本没在跑」
> 区分的绿，不算绿**。用例集里同样有 6 条合格条目在要求闸门放行。
>
> **用例文件本身也要被校验，其中最要紧的一条是语义一致性**：
> `kind` 与 `expect` 里的失败原因必须对得上（`overlong` 必须期望 `too_long`，
> `clean` 必须期望零失败）。这条拦住的正是最危险的一种坏用例 ——
> **标错标签的用例**：它看起来在覆盖某个场景，实际在测别的东西，
> 而 `"kind": "overloug"` 这种 typo 本来不会让任何东西报错，
> 只会让用例静默地什么都不测。
>
> **覆盖率自检查两件事：总数与每一种场景都有。** 37 条全落在
> `hallucinated_number` 上也能凑够数量 —— 一个场景堆量，其余六个一条没测。
>
> **反向验证组（6 条）专门证明「评测器有能力发现问题」**：
> 篡改期望 → 必须报出来；把期望的数字改掉 → 必须报「漏报」；
> 少写一条期望 → 必须报「多报」而不是静默通过；
> 标错标签 → 必须被结构校验拦下；少一类场景 / 用例数不足 → 必须被覆盖率自检拦下。
> 前面的绿只说明「当前没有已知问题」，这一组才说明「这个判据不是永远绿的」。
>
> **用例装载用 `import.meta.glob`，不用逐行 `import`。** 两种写法都能跑，
> 差别在于**加一个用例文件要不要改代码**：逐个 import 的写法里，
> 忘掉一行就等于那个文件永远不执行 —— 而它的症状是「评测集全绿」。
>
> **清单之外另加：**
> - **确定性断言**：同一批用例跑两遍，结果逐字相同。这是 AGENTS.md §7
>   「同一 `(profile, JD)` 通过与否高度一致」唯一的实现方式。
> - **一个用例可以期望同一原因出现两次**（`hallu-06`：一条里编了两个数字）。
>   执行器对带 `offending` 的期望做**消耗式匹配** —— 不消耗的话，
>   两条期望会同时匹配到第一条实际失败，第二条的真假就没人查了。
> - `offending` 在期望里是**可选**的，只在它是考点时才写（「编的那个数字是 40」）。
>   若要求必填，每条用例都要依赖闸门的错误文案细节，改一次文案会引发几十处
>   与考点无关的修改 —— 而**文案不是被测对象**。
> - 用例文件按文件名排序后汇总：报告顺序只取决于文件名，不取决于文件系统。
>
> **新增 `core/verify/parity.ts`（跨语言数字一致性）—— T3.4 需要它，T4b 会复用。**
> TASKS.md 原本把这条列在 T4b，而 T3.4 勾着「覆盖跨语言不一致」，
> 所以原语建在这里。判据是**全集合相等**，比 T4b 写的「按类别两两相等」更强
> —— 集合相同 ⇒ 任意类别划分下都相同，且不必先去定义「哪些数字是时间、
> 哪些是金额」，而那个分类本身就是一个会漏、会漂移的启发式。
> 代价认领：全集合相等会误伤「91%」这种**正确的换算**。这是有意接受的 ——
> 跨语言改写要一致的是事实，不是算式；想让某个换算通过，正确做法是两份里
> 都用原始数字，而不是放宽闸门。**T4b 执行时请确认这条替代关系，或推翻它。**
> 顺带把 `compareNumeric` 从私有改成导出：`parity.ts` 与 `untraceableNumbers`
> 都要输出数字列表，两处各写一次 `sort()` 会让同一个界面上出现两种排序。
>
> **一处刻意放宽的 tsconfig：`packages/evals` 的 `types: ["vite/client"]`。**
> `core` 的 `types: []` 是它自己的约束（要证明零运行时依赖），
> 评测包是开发期工具，不承担那条约束 —— 它需要 Vite 的 `import.meta.glob` 类型。
> 这不是把闸门放松，而是承认两个包的性质不同。
>
> **`packages/evals` 用相对路径 import core，而不是 `@applypack/core`。**
> 本仓库路径下 pnpm 建不了符号链接（实测记录见 `pnpm-workspace.yaml`），
> 所以走相对路径。这不是权宜之计：`core` 的 `exports["."]` 指向的正是
> `./src/index.ts`，符号链接只是把同一个文件换个名字暴露出来。
> 理由写在 `cases.ts` 的文件头与 `package.json` 的 `description` 里。

---

## M4 · 渲染与投递包

### T4a 模板渲染 + PDF 导出 ✅ 已完成（PDF 生成部分见下方说明）
产出：`core/render/`
- [x] 断言姓名 / 学校 / 公司 / 日期齐全且顺序正确
- [x] 该断言是「ATS 可解析」的唯一可信证据
- [x] PDF 生成与 pdfjs 反向抽取 —— 见下方 **T4a-PDF**（属 `packages/web`，选 react-pdf）

> 21 条断言，与前面合计 349 条；`pnpm test` 与 `pnpm typecheck` 均通过。
> **到这一条为止，`§0` 的竖切链路 `T1.1 → T1.2 → T2.1 → T2.2 → T3.2 → T4a` 全部打通。**
>
> **一处必须认领的取舍：没有 `pdfjs` 反向抽取，因为没有 PDF。**
> PDF 生成需要浏览器环境（react-pdf / 打印 CSS），而 `core` 是零 DOM 的。
> 所以本层做的是 `core/render/`：文档模型（排版无关）+ HTML 渲染（纯字符串）
> + ATS 文本抽取（纯字符串）。**PDF 生成与对它做反向抽取，属于 `packages/web`** ——
> 那个包在 T2.3 才建起来（Vite + React + Tailwind），本任务当时确实顺带不了。
>
> **这个缺口是有边界的，而且边界被论证过，不是「用 HTML 冒充 PDF」：**
>
> | | 保证 | 不保证 |
> |---|---|---|
> | 本层断言成立 | 内容齐全、顺序正确、文本不依赖样式表达 | PDF 生成器没有把文本层搞乱 |
>
> 后者的风险靠 `FORBIDDEN_LAYOUT_PATTERNS` 压到很低：只要产物是单栏、
> 无绝对定位、无表格布局，「阅读顺序 == DOM 顺序」就是排版引擎的必然行为。
> **分栏才是 PDF 文本层错乱的唯一常见来源，而它被一条断言挡住了**（T4c 把这份清单做成了扫描器，见 T4c 的 ③）。
> 剩下的一小块，等 Web 端接入 pdfjs 时用同一条断言补上 —— 断言本身现在就能写，
> 只是被断言的对象还没生成出来。
>
> **本层与 `core/egress` 构成一组对照，这组对照是 DESIGN 1.3 的完整实现：**
> `egress` 断言「同一个手机号绝不出网」，`render` 断言「同一个手机号必须印在简历上」。
> 两条同时成立，才说明 B 级管的是「进不进模型请求」而不是「能不能上简历」。
>
> **渲染产物是纯字符串，所以「ATS 可解析」这件事在 CI 上可验证，不需要跑浏览器。**
> 这是零 DOM 约束带来的一处真实收益，不是妥协 —— 一个需要浏览器才能跑的
> ATS 断言，会在第一次超时时被 `skip` 掉。

### T4a-PDF PDF 生成与 pdfjs 反向抽取 ✅ 已完成（packages/web）
产出：`web/src/features/delivery/`（document / fonts / pdf-text / verify / render-node / render-browser + delivery.test.ts）+ `scripts/fetch-fonts.mjs`
- [x] PDF 生成：`@react-pdf/renderer`，**同一 `DocumentModel` 的第二个渲染后端**（不是 HTML 转 PDF）
- [x] pdfjs 反向抽取：**PDF 文本层 == `PackageView.texts`（HTML 的 ATS 文本），逐字符**，忽略全部空白与已声明的装饰（`●`）
- [x] 装饰机制活着：`●` 出现次数精确等于要点行数；未声明的多余字符必须为空
- [x] 页数估算第一次被实测对照：campus 三版实测 1 / 1 / 2 页；**「估算只可能高报」从读代码确认升级为断言**（maximal 估算 2 ≥ 实测 2）
- [x] 双语拼页在成品上精确：`bilingual == en + cn`（与 `package.ts` 同一条判据，输入换成产物）

> **web 包合计 43 条**（T2.3 的 18 + 本节 25）；全仓 `turbo typecheck test build` 9/9 绿
> （core 658 / extension 46 / evals 11 / web 43）。
>
> **① 选型先 spike 后实现。** 打印 CSS 出局的理由（TASKS.md:762）：产物字节不进 JS，
> 文件名控不了、页数量不了、回归在 CI 里不可见；`html2canvas` 一类出局：栅格化毁掉文本层。
> spike（`renderToBuffer` + pdfjs 抽取，中文不走样、字体子集 11 KB）验通后才写模板。
>
> **② 字体：内嵌，钉哈希，不入库。** Noto Sans SC 静态 TTF 两个字重（400/600，
> PDF 不能合成伪粗体），21 MB 由 `fetch-fonts.mjs` 按 SHA-256 在 pretest/prebuild 拉取，
> OFL 声明随字体落盘。浏览器与 Node 各给一种 src 形态（`fonts.ts`），幂等登记。
>
> **③ 本轮抓到的五个真问题（每个都改变了实现）：**
>
> | # | 症状 | 根因 | 修法 |
> |---|---|---|---|
> | 1 | 标记 `•` 抽回来变成 `·`，被判成「未声明的多余字符」 | Noto Sans SC 把 `•`(U+2022)、`·`(U+00B7)、`･`(U+30FB) 编成**同一字形**（glyph 1375），子集化后 ToUnicode 只能映射回一个码点；正文里真 `·`（meta 分隔符）一出现，标记就丢失了自己的身份 | 标记改用 `●`(U+25CF)——它在字体里有**独占字形**（glyph 1000）。选装饰字符前必须查反向映射唯一 |
> | 2 | pdfjs 抽取永远挂起，beforeAll 超时，看着像「渲染慢」 | pdfjs 6 默认构建的 `MessageHandler` 用 `Promise.try`（ES2025），Node 22 没有；报错发生在消息层，主 Promise 不 resolve | 走 **legacy 构建**（`pdfjs-dist/legacy/build/pdf.mjs`，自带 core-js polyfill），不打全局补丁 |
> | 3 | 成功的抽取在 `finally` 里被顶成 `TypeError` | pdfjs 6 的 `destroy()` 在 **loading task** 上，文档代理上只有 `cleanup()` | 对正确的对象调 destroy |
> | 4 | 测试全绿但 typecheck 红 | react-pdf 4.9 没有 `<Document/Page hyphenationCallback>` prop，连字符要在 `Font.registerHyphenationCallback` 全局设 | 移入 `fonts.ts` 登记处；**两道门都要跑**（关连字符是文本层判据的前提：连字符 `-` 不在装饰集里） |
> | 5 | 「估算 685.8 / 762.5 pt（90%）」却测得 2 页 | **测试自己的 bug**：`rendered` Map 以 `view.name` 为 key，两个 fixture 的视图同名（`Resume_CN` 等），campus 的渲染被 maximal 覆盖，页数断言全在跑错的产物 | key 改为视图对象本身。这条没被任何断言抓住，是被「估算远小于容量却溢出」的矛盾逼出来的 —— 交叉证据值得 信 |
>
> 另有一处 core 侧补齐：`entryLines` 加入 `core/render` 导出面 —— react-pdf 把组件
> 内部错误吞成 `null`，探测时只有把它单独 import 才能看到真错。三个消费者
> （HTML 渲染 / 页数估算 / PDF 模板）从此共用一个产地，「PDF 少印一行」没有可发生的地方。
>
> **④ 变异验证（判据先证明自己会红）：** M1 删掉要点标记 → 装饰计数红；
> M2 双语版颠倒中英顺序 → 3 处红（逐字符比对 / 拼页等式）。
>
> **⑤ 判据的已知缺口（写下来而不是藏起来）：** 排版引擎在空格处断行会吞掉那个空格，
> 所以按空白归一化后，「`Campus Marketplace` 被渲染成 `CampusMarketplace`」能通过
> 主判据。补偿在 core 侧：文本的空格正确性由 T4a 本层的断言守着。逐条比对。


### T4b 三版投递包 ✅ 已完成
产出：`core/render/package.ts` + `layout.ts` + `paginate.ts`（附 `verify/parity.ts` 的一处新增）
- [x] `Resume_EN` / `Resume_CN` / `Resume_Bilingual` 一次产出
- [x] 跨语言数字集合（时间 / 金额 / 百分比 / 规模 / 名次 / 语言分数 / GPA）**全集合相等** —— 确认，取代原写的「按类别两两相等」
- [x] 篡改英版任意一个数字 → 校验必须失败
- [x] EN 版页数 == 1（超页判失败）
- [x] 教育段落在 EN 版中位于实习经历之前

> 66 条断言（`package` 30 / `paginate` 36），与前面合计 514 条；
> `pnpm test`、`pnpm test:evals`、`pnpm typecheck` 三个包全绿。
>
> ---
>
> **① 「按类别两两相等」被确认取代，不是被推翻。**
> 原话要求按（时间 / 金额 / 百分比 / …）分类比较。`parity.ts` 用的是**全集合相等**，
> 而它是前者的**充分条件**：集合相同 ⇒ 任意类别划分下都相同。所以判据更强，
> 也更容易解释 —— 不必先去定义「哪些数字是时间、哪些是金额」，
> 而那个分类本身就是一个会漏、会漂移的启发式。
> 代价（同一份文件里已认领）：会误伤「91%」这种正确的换算。有意接受。
>
> **② 对清单的一处实质修正：双语版不进「两两相等」这组比较。**
> 把它放进去是一个**看起来更严、实际更弱**的做法，两头都错：
>
> - **更弱：** 双语版的数字集合是两半的并集。若「双语版只剩了 EN 一半」，
>   它的集合恰好等于英版 —— 两两比较全绿。它看起来在覆盖「拼页有没有拼全」，
>   实际一条都没覆盖。`package.test.ts` 里有一条断言专门造出这个反例：
>   先把双语版砍成只剩英版、断言三对 `textsParity` **全部通过**，
>   再断言真正守着这件事的 `bilingual_incomplete` 必须红。
> - **更冗余：** 英版与中版本来就要求相等（否则整包已失败），
>   于是双语版与它们的比较是同一个结论的第三次复述。
>
> 双语版改由两条**它才是唯一执行者**的检查守着：完整性（两半的每一行都在）
> 与顺序（英版是前半段）。这个分工写在 `package.ts` 的文件头 ——
> 「哪条检查守哪件事」正是最容易在后续改动里被挪错的东西。
>
> **③ 页数不是测量，是估算 —— 但误差的**方向**是被选的。**
>
> 真实的页数只有排版引擎知道，而 core 里没有排版引擎也没有字体。
> 所以这里做的是确定性高度累加，而近似的每一侧都朝**同一边**偏：
> 字形宽度取宽（0.55 em）、不做断词、块的清单与渲染块一一对应（有断言）。
> 结论是**它可能说「超页」而实际没超，不会说「刚好」而实际超了**。
> 误报的代价是删一两个要点，漏报的代价是一份两页的简历投出去。
> 这个不对称是它敢当闸门的全部理由，也是它必须被 pdfjs 实测替换的理由 ——
> **只改常量是不够的，也不该够。**
>
> 一个意外的对称：**拼页时页数估算从近似变成精确**。双语版的两半各自从新页
> 开始（`break-before: page`），不存在跨页断点，所以 `pages = pages(EN) + pages(CN)`
> 在那里是精确的，而单份文档上的同一个 `ceil` 只是量级。
>
> 校招样本实测：EN `used=701.5pt / cap=762.5pt`，**余量 61pt（3.87 行）** ——
> 有富余但贴着边界，这正是这条断言该在的位置（余量上界被断言压到 8 行以内，
> 否则「一页」这条断言会在一个空文档上也成立）。
>
> **④ 一个被自己写的断言抓住的恒真判据（本轮最值钱的东西）。**
> `overflowPt` 第一版写成 `used − pages × 单页容量`。看起来对，实际恒为 0 ——
> 因为 `pages` 本来就是 `ceil(used / 单页容量)` 算出来的。于是失败信息里
> 会写着「嗯，超出 0pt」，而那时它已经超了一整页。
> 断言 `expect(estimate.overflowPt).toBeGreaterThan(0)` 当场变红。
> 修法是把基准改成**单页容量**（「离一页这条线还有多远」），
> 而不是「这些页加起来能装多少」。
> 这是同一形状的第 5 次（零条 bullet 的 `pass === true`、AAD 作用域、
> `policy.ts` 穿过数组的静默漏报、`loadArchive` 读了旧信封）。
>
> **⑤ 三态判据被自己的测试推翻，改成了两态 + 一个布尔。**
> 初稿把跨语言一致性分成 `consistent` / `inconsistent` / `nothing_to_verify`，
> 第三种想要表达「数字相等，但那些数字全来自两侧逐字相同的文本，什么也没验出来」。
> 测试跑出来：**那个状态在实践中到不了** —— 联系方式行在两种语言里标签与分隔符
> 不同，而它带着手机号，于是永远有「独立数字」。断言红了两条。
> 与其把判据调得能凑出那个状态，不如承认它判错了东西：
>
> - `independentCn` / `independentEn` 留着 —— 它是**校验的实际作用范围**，这个信息有用；
> - 真正要报警的是**要点是否同源**。`highlights` 是一个数组两语共用，
>   没跑英文改写时两版要点逐字相同，于是「要点里的数字一致」是同一份文本
>   被渲染了两次。所以加了 `bulletsFromSharedSource: boolean`。
> - 它**不是失败**（档案没问题，缺的是一个动作），但也**不许**被说成
>   「跨语言一致性已通过」—— 界面上必须分开说。
>
> 顺带一个真实的收益被测试照出来：联系方式行的两次渲染虽然弱，但**确实独立**——
> 英版里把手机号打错一位会被这条校验抓到，而那是肉眼最难发现的一类错。
>
> **⑥ 为了让「一行」有意义，改了三处渲染。**
>
> | 改动 | 为什么必须 |
> |---|---|
> | 加 `@page { size: A4; margin: 14mm }` | 原来只有 `padding`，打印时第二页起没有上边距；而且正文宽度是「`max-width` 与 padding 相加」这个说不清的数字。现在它是可计算的 182mm，也是页数估算的可用宽度 |
> | `.summary` 补上显式边距 | 它是唯一一个靠浏览器默认 `p { margin: 1em 0 }` 的元素。**参与高度估算的块，边距必须显式写出来** —— 否则估算器得去猜 UA 默认值，而那是藏在算式里的假设 |
> | 联系方式分隔符按语种 | 英文简历里出现 `Email：foo@bar.com` 这种全角标点是「中文模板套英文内容」最典型的痕迹，而 10.1 整节就是在反对这件事 |
>
> 排版常量集中在 `layout.ts`（唯一真相源），CSS 手写、常量手写，
> **由 20 条漂移断言把两者的数字钉在一起** —— 与 T2.1 的 `schema-snapshot.test.ts`
> 同一个取舍：不做代码生成（那会把样式表变成不可读的模板），而是让不一致必然报错。
>
> **⑦ 反向验证（T4b 第三条勾）做的是「每一个」，不是「某一个」。**
> 枚举英版文本里的**每一处**数字位点（实测 16 处），逐个换成哨兵值 `987654`
> —— 哨兵必须是原本不存在的数字，否则换成一个已出现过的数字时集合仍然相等，
> 那条断言会因为巧合而通过。同时断言位点数 ≥ 12：一条 `for` 循环里没有元素时
> 也会「通过」，这是 T1.4 那次教训的又一次应用。
> 另有两条配对断言：未篡改的基准必须全绿，`cross_language_mismatch` 必须是 `fatal`。
>
> **⑧ 一处向后兼容的输出变更，需要写下来。**
> 英文联系方式行从 `Email：x` 变成 `Email: x`（全角冒号 → 半角冒号加空格）。
> T4a 的 21 条断言一条没改就通过 —— 因为它们断言的是模型字段与文本内容，
> 而这次改的是分隔符。**这属于「改了但仍兼容」，不是「没改」。**
> 中文版不变。
>
> **清单之外另加（都有断言）：**
> - 双语版必须是**一份**文档：只有一个 `<html>` / `<head>` / `<body>`。
>   把两份完整 HTML 首尾相接浏览器能容错渲染，而 pdfjs 与各类 ATS 解析器不一定。
> - 两半各带自己的 `lang`：根节点只能声明一种语言，中文半页不带标记会让
>   屏幕阅读器与拼写检查按英文规则读中文。
> - 断页属性挂在**后**半上而不是文档末尾 —— 以断页属性结尾时部分渲染引擎会多吐空白页。
> - 双语版文本 == 英版 ++ 中版，逐字相同且顺序如此（这条同时是顺序检查的依据）。
> - 中文版**不设页数上限**：把英美的规范套到中文简历上正是 10.1 反对的方向。
>   满档案的中版 2 页且不产生任何失败。
> - `verify/parity.ts` 新增 `locateNumbers`：「数字 6 不一致」等于没有报错，
>   失败信息必须落到**行**上（`6（「CET-6」）`）。纯新增，T3.4 的断言一条没改。
>
> **当时仍未做（已由 T4c 关闭）：** 文件名的 `^[A-Za-z0-9_\-]+\.pdf$` 断言、
> 以及基于 `FORBIDDEN_LAYOUT_PATTERNS` 的双语非并排断言 —— 后者本任务已有一半
> （`renderBilingualHtml` 里没有分栏、样式表漂移组里断言了禁止清单）。
> T4c 的做法是把它做成一个**扫描器**（`format.ts` 的 `scanForbiddenLayout`）
> 而不是再加一条 `not.toContain`：那个空格问题见 T4c 的 ③。

### T4c 文件名与格式规范 ✅ 已完成
产出：`core/render/filename.ts` + `core/render/format.ts`（附 `html.ts` 禁止清单的硬化与拆分）
- [x] 输出文件名匹配 `^[A-Za-z0-9_\-]+\.pdf$`（无中文、无空格）
- [x] 双语为**顺序拼页**，断言不存在双栏布局

> 52 条断言（`filename` 22 / `format` 15 / `package` 追加 15），与前面合计 567 条；
> `pnpm test`、`pnpm test:evals`、`pnpm typecheck` 三个包全绿。
>
> ---
>
> **① 对清单的一处实质补强：文件名必须是「输入的函数」。**
> T4c 原文只有一条判据。若三份文件名是写死的常量，那条判据就是
> 「对常量做正则匹配」—— 一条**恒真**断言，无法与「检查根本没在跑」区分开。
> 所以 `buildDeliveryPackage` 接受一个 `fileStem`（招聘系统常要求
> 「姓名_岗位.pdf」这种命名），词干经净化后才拼进名字，后缀由代码追加。
> **默认路径与 T4b 的视图名逐字相同**（`Resume_CN.pdf` / `Resume_EN.pdf` /
> `Resume_Bilingual.pdf`），有一条断言钉住这个兼容性。
>
> 这个分工带来一条**可证的命题**：三份文件不可能撞名 —— 后缀在净化之后追加，
> 所以不同的后缀必然给出不同的名字，与词干是什么无关。
> 把「一类失败变成不可能」比「检测它」更好，所以这里**没有**撞名检查，
> 只有一条证明那个分工还在的断言。
>
> **② 「张智远」是中文用户最可能的输入，而它净化后什么都不剩。**
> 回落本身是对的（没有 ICU，拼音化会把名字写成错的），但**必须说出来**：
> 文件生成成功、名字合规、没有任何报错，而用户拿到的名字不是他要求的 ——
> 这正是「看起来在工作」那一类问题。所以 `naming` 把
> `requestedStem`（要求了什么）与 `usedStem`（实际用了什么）分开返回，
> `fellBack` 显式标出回落。**它不是失败**，`check.pass` 不受它影响。
>
> **③ 双栏这一条不能只做「子串里有没有 `column-count`」。**
> 三处硬化，每一处都有反向验证：
>
> | 硬化 | 为什么 |
> |---|---|
> | 匹配前 `normalizeCss`（小写 + 去全部空白） | `display: flex` 不含 `display:flex`。这一条不是假想的：把 `.contacts li` 的 `display: inline` 改成 `display: flex` 后，`ats.test.ts` 里那条朴素的 `not.toContain` 循环**一条都没红**，红的只有硬化后的扫描器 |
> | 只扫**样式面**（`<style>` 块 + `style` 属性） | 扫整个产物会让正文里出现 `display:grid` 的样子（一段讲前端的项目描述）被判成「排版不合格」 |
> | 标签类禁止项扫整个文档 | 这里不会有假阳性：**渲染层对一切内容做了转义**，正文里的 `<` 一定是 `&lt;` |
>
> 禁止清单同时拆成两类：`FORBIDDEN_CSS_DECLARATIONS`（新增
> `display:flex` / `display:inline-flex` / `display:inline-grid` /
> `grid-template-columns` / `@import`）与 `FORBIDDEN_MARKUP`
> （`<table` / `<link` / `<script` / `<iframe` / `<object` / `<embed`）。
> 原来的 `FORBIDDEN_LAYOUT_PATTERNS` 保留为并集，供「整份产物里有没有」
> 的粗检使用 —— **T4a 的断言一条没改**。
> 顺手删掉了 `'position: absolute'` 这类「写法的变体」条目：白名单式的
> 形式变体是错的，那该由归一化解决，而不是往清单里堆。
>
> **④ 双语版的第三条检查：一页多出来的一行。**
> T4b 的两条（缺行 / 顺序倒了）在全绿的情况下，双语版仍可能不是**逐字**拼接。
> 两种情形，都有断言：
>
> - **多出了行**（页眉、页码）：`missing` 空、`extra` 非空、逐位比较也发现不了；
> - **后半内部被换过顺序**：行数相同、多重集相同，`missing` 与 `extra` **都是空的**，
>   只有 `firstMismatch` 抓得到。
>
> 所以 `stitchReport` 给出三个**互不蕴含**的信号，`bilingual_not_sequential`
> 在「既不缺行、英版也在前、但仍不 ok」时触发。
> `package.test.ts` 里有一组表：缺尾 / 倒序 / 多行 / 后半换序，
> **每一个都只触发一条**，另有一条断言证明完好的双语版三条都不响。
> 分工写在 `format.ts` 与 `package.ts` 的文件头 —— 「哪条检查守哪件事」
> 是最容易在后续改动里被挪错的东西。
>
> **⑤ `checkFileName` 在真实路径上到不了，所以它必须被手工喂一次。**
> 净化器的契约是「任何输入都产出合规文件名」（对抗性输入表 22 项证明了它），
> 于是文件名失败是一条**回归绊线**：它响的时候说的是「我们自己的契约被绕过了」，
> 不是「用户填错了」。它必须仍然能响 —— `package.test.ts` 手工构造
> `张智远 简历.pdf` 与 `Resume_CN`（缺后缀）各一条，并断言前者报
> 「第 1 个字符「张」」、后者报「后缀必须是 .pdf」而不是「第 N 个字符」。
> 后缀单独先判：`Resume_EN.pdf ` 报到第 10 位的 `.` 会把人领到错的地方。
>
> **⑥ 变异测试（本轮的反向验证）。** 四处故意破坏，各自被抓住：
>
> | 变异 | 抓住它的断言数 |
> |---|---|
> | 净化器只去空格（`/ +/g`） | 6（含「全中文词干必须回落」） |
> | 「多出一行」不再触发 | 2 |
> | `.contacts li` 改成 `display: flex` | 8（朴素检查 0） |
> | `checkFileName` 恒返回 `null` | 4（含 `checkPackage` 里那两条，证明接线是活的） |
>
> 三份产物 × 两份样本（校招 / 满档案）的单栏扫描全部为空；
> 双语产物的两半是 `<body>` 的**直接子元素**，没有包裹层 ——
> 并排两栏需要一个 flex/grid 父容器，所以「没有包裹层」是结构上的第二道防线。
>
> **当时未做（不属于本任务缺口）：** `T4d` 自述三视图 —— **已在下一节完成**；
> `T4a` 的 PDF/pdfjs 部分（属 `packages/web`，**该包已在 T2.3 建起来**，现在是可做的了）。
> 文件名进入 Web 端的下载/上传调用属 M5/M6。

### T4d 自我介绍三视图
- [x] `intro_zh@60s` / `intro_en@60s` / `intro_en@200w` 共用同一事实源
- [x] 数字集合纳入 T4b 的统一校验

> **① 「共用同一事实源」做成了一条类型约束，不是一句承诺。**
> `introFacts` 的入参不是 `ArchiveV1`，是**已经建好的两个 `DocumentModel`**
> （`resume_zh` 与 `resume_en_campus`，与简历用的是同一个 `buildDocumentModel`）。
> 取值、i18n 回退、节顺序、改写后要点在建模时发生过一次，这里只是重新组织成句。
> 所以「改完简历忘了改自我介绍」这件事**没有可以发生的地方** ——
> 事实源模块拿不到档案，也就读不到模型之外的字段。
> 一条断言把这件事钉住：三档的 `factIds` 是同一个 `facts` 序列的子集且保序，
> 且 `facts` 不随请求的档位变化。
>
> **② 「数字集合相等」从判据降级成了绊线 —— 因为它变成了构造性的。**
> 选择事实时，**带数字的事实同进同出**：任一侧装不下，两侧都不收。
> 于是「中英两档数字集合相等」在结构上成立，那条校验退化成抓**模板漂移**
> 的回归绊线（某次改动让英文模板丢掉了 `2021.09`，两边都读得通）。
> 不带数字的事实允许不对称 —— 这是「英版别被中文的篇幅拖住」的唯一手段，
> 也是 DESIGN 10.2 那条判据仍然可能是真判据的原因。
> 两条都要有：`intro.test.ts` 手工构造一次漂移喂给绊线，也手工构造一次
> 「一侧装得下、一侧装不下」证明同进同出真的在跑。
>
> **③ 这一轮抓到的最有价值的东西：`targets.ts` 里那个假设是假的。**
> 原文写着「240 字与 150 词承载的信息量大致相当（1 汉字 ≈ 0.6 英语单词），
> 所以两个 `@60s` 是同一档」。同一份事实源、同一批事实，两种语言各渲染一遍，
> 实测是 **209 字 ↔ 79 词 = 0.38 词/字**。于是「60 秒」在两种语言下
> 指向不同量的事实：
>
> | 档位 | 预算 | 样本上填到 |
> |---|---|---|
> | `intro_zh@60s` | 240 字（档 216–264） | 209–214 字 = **87–89%**（差 2–5 字掉出档外） |
> | `intro_en@60s` | 150 词（档 135–165） | 64–79 词 = **43–53%** |
> | `intro_en@200w` | 200 词（档 180–220） | 64–79 词 = **32–40%** |
>
> **三种样本（校招轻样本、带改写、满档案）三档全部掉出档外。**
> 两种解释都说得通，量级差二十倍：
> **(a) 常数问题** —— 150 wpm 是演讲语速，而转述简历要点更接近口语的 130 wpm；
> **(b) 内容问题** —— 一页校招档案里可朗读的内容本来就不够一分钟，
> 中文侧看着接近只是因为 4 字/秒算得偏松。
>
> **✅ 已拍板（2026-09-19，fiber）：常数不动，缺口按内容问题处置。**
> 0.6 与 0.38 量的不是同一个东西：0.6 是语速比的产物（时间维），0.38 是
> 本项目渲染产物的内容密度（被「英版删非数字事实」污染，n=1）——**不拿它当
> 换算常数**。决定性算术：即使按忠实 0.6 折算，209 字也只有 125 词，
> 仍低于 `intro_en@60s` 档下限 135 —— 选哪个常数轻样本都过不了，主导项是内容。
> 落法：语速常数 {4.0, 2.5} 不动；`check.ts` 照 ±10% 判，轻样本诚实报缺
> （`shortHints` 按代价给「先改写 / 再补事实」）；未来要填缺口动的是
> DESIGN 10.2 的删事实尺度，不是常数。详见 `targets.ts` 2026-09-19 注记。
> `intro.test.ts` 里有一条「校准温度计」把这三个数钉成断言 ——
> 它不是判据，是让改动可见：改模板、改切句、改样本时它会动。
>
> **④ 五个 bug，前四个都是「名字说了谎 / 判据没在跑」。**
>
> | # | 症状 | 根因 | 怎么被发现 |
> |---|---|---|---|
> | 1 | 三档同时报「自述里的数字简历上没有」，连 `4.2` 都报 | 入参叫 `ResumeNumbers` 却装着一行行**正文**，拿数字去 `Set.has` 整行文本，恒不命中 | 探测跑通后一屏全红；修法是**改名**为 `ResumeTexts` + 显式跑一次 `extractNumbers` |
> | 2 | 「英文口播句均 ≤ 25 词」永远不会响 | `splitSentences` 的 `\s*` 允许零宽，于是**每一个小数点都是句边界**（8 句被切成 13 句），句均被稀释 | 探测里数出的句数比人眼数的多 |
> | 3 | 带换行的文本凭空多出几个「字」 | `NON_UNIT` 只列了 `\p{P}\p{S}\p{Z}`；`\t` `\n` 属 **`\p{Cc}`** | 单元测试写「一 二\t三\n四」得 6 而不是 4 |
> | 4 | `education.0.0` 在 `factIds` 里出现两次，静默丢一条事实 | 学历事实与「无要点时的兜底事实」算出了同一个 id | 探测打印 id 列 |
> | 5 | 英文自述念出「At Campus Marketplace Recommender, I 推荐系统 · 协同过滤.」 | `model.ts` 把技术关键词 `unshift` 进了 `bullets`（为 ATS 子串匹配），自我介绍把它当成一条正常要点念 | 探测打印文本 |
>
> 第 5 条改的是 `DocumentModel`：新增 `keywordLine` 字段，`bullets` 不再含那条标签串，
> 渲染与页数估算**共用** `entryLines()`。产物的 HTML 与 `texts` **逐字节不变**
> （T4a / T4b / T4c 的断言一条没改，这是它没被改坏的唯一证据）；
> `crossLanguageParity.bulletsFromSharedSource` 的口径也刻意保持用 `entry.bullets`。
> 与它同类的一条判断：技能节的 `keywords` **不**搬进 `keywordLine` ——
> 项目的关键词是标签串（不可朗读），技能的每个关键词是一个「会什么」（可以念）。
> 同名不同物，所以下游处置必须分开。
>
> **⑤ `no-async.guard.ts`：把「不出网」从承诺变成类型事实。**
> 一次真实网络调用必然是异步的，所以「`buildIntroViews` 的返回类型不是
> `Promise`」这条类型事实，等价于「它不可能等待任何一次网络调用」。
> 四条 `@ts-expect-error` 与 `no-runtime-deps.guard.ts` 同款，但**多一个阳性对照**：
> 一个 `declare function asyncProbe(): Promise<void>` 走同一个宏，
> 必须得到 `true`。没有它的话，那四条有可能只是因为宏坏了（恒返回 `false`）
> 而全绿 —— 那样这个守卫什么都没证明。
> 它证明的是「没有 `await`」，不是「没有 `fetch`」；后者由依赖清单保证
> （本模块只 import 纯模块）。这一句写在文件头，免得被当成更强的保证。
>
> **⑥ 仍未做 / 待拍板。**
> - **中英语速常数（`targets.ts` 的「已知问题」）**：见 ③。拍板前 `@60s` / `@200w`
>   这两档对真实档案是红。这是一个需要产品判断的决定，不是代码缺口。
> - **终稿的口播 / 书面语体**：本模块产出的是**草稿**（「可朗读的要点稿」），
>   让它读起来像人说话的那一步与 T3.1 同款（一次模型调用），且**润色不得引入新数字** ——
>   那正是 `intro_unsupported_number` 守的东西（`fatal`，已反向验证）。
>   顺序不能反：先确定性成稿再润色 ⇒「编数字」在草稿阶段不存在；
>   先自由写再校验 ⇒「不许编数字」变成一句提示词。
> - **`intro_en@500w` / `@3min` 尚未被端到端跑过**：八个档位都在表里、合成器与校验器
>   与档位无关（断言证明 `facts` 不随档位变化），但没有针对长档的用例。
> - **T4a 的 PDF/pdfjs 部分**已完成（T4a-PDF 一节）：选型定为 react-pdf，
>   判据「PDF 文本层 == HTML 的 ATS 文本」已在成品上逐字符成立。

---

## M5 · 扩展与填充

### T5.1 内容脚本 + 适配器注册表 ✅ 已完成（纯逻辑层）
产出：`packages/extension/src/fill/`（7 个模块 + 30 条断言）
- [x] 单测跑在保存的 HTML fixture 上，**不依赖真实站点**
- [x] 字段匹配（启发式兜底）有准确率断言

> 沿用 T1.5 的模式：**纯逻辑层先行，平台接缝留最小接口**。
> 整层对 DOM 只读不写、输入输出全是纯数据，`environment: 'node'` 不变 ——
> 真实 DOM 与 jsdom 只需满足 `dom.ts` 的最小结构接口
> （`querySelectorAll` / `getAttribute` / `textContent` / `getElementById`），
> 谁也不 import 谁。WXT 骨架与 `chrome.*` 接线属 T5.3 及以后；
> `tsconfig` 的 `types: []` 让 `chrome` 全局不可写。
>
> **分层（每层只对一件事负责，各有各的判据）：**
>
> | 模块 | 职责 | 关键判据 |
> |---|---|---|
> | `features.ts` | 页面 → 字段特征（文案线索/类型/必填） | label[for] 与 aria 两种来源、hidden/submit 排除、key 唯一 |
> | `catalog.ts` | 可填字段词汇表（14 条：path/level/同义词/类型闸门/取值） | level 与 core 分级策略逐条一致（`fieldPolicyFor`） |
> | `heuristics.ts` | 评分器：标签 10/6 · name 7/4 · placeholder 5/3，类型硬闸门 | tel 输入框不可能接 email（正控 + 负控） |
> | `adapters.ts` | 注册表 + mockboard 虚构适配器（T5.2 换真实平台） | 选择器扑空 = 静默降级，不报错 |
> | `plan.ts` | 组装 `FillPlan` 判别联合（fill / gap / file-slot） | 见下 |
>
> **主判据是逐字段 ground truth 双向相等**（不是准确率百分比）：
> 13 个期望填的字段 key→path 逐项相等、5 个期望缺口的原因逐项相等 ——
> 多填（错配）与少填（漏配）都红。项目立场「填错比不填更糟」
> 在断言里的形状是：**歧义 = 一个都不填**（两个同标签同类型字段打平 → 全部 gap）。
>
> **缺口语义五分**（T5.3 的预览 UI 直接消费）：
> `no-catalog-match`（页面字段不认识）/ `no-value`（目录认得但档案是空的 ——
> DESIGN 11 的真缺口）/ `ambiguous` / `outbid`（有信号但输给了别的字段，
> 如「紧急联系人电话」对上手机）/ `option-mismatch`（档案有值但下拉选项里没有）。
> `blocking` 随 gap 走：required 字段的缺口即时标记（DESIGN 11.2 例外条款）。
>
> **实现期抓到的两个真问题（都被自家判据逼出来）：**
>
> 1. **短英文同义词的子串误报**：`name` ⊆ `candidate_name` 让「纯启发式救不了」
>    的测试字段（适配器对照组）被启发式意外填上。修法：≤4 字符的纯 ASCII
>    同义词只做精确匹配 —— 英文没有中文「字面包含 ≈ 语义相关」的性质。
>    中文同义词保留包含匹配（`手机` ⊆ `手机号` 是有价值的信号）。
> 2. **精确同义词三方打平**：fixture 里「手机号」与两个「联系电话」都是
>    `basics.contact.phone` 的精确同义（各 10 分）→ 三方歧义 → 主字段也没得填。
>    这不是评分器的 bug，是 fixture 没有表达「主字段 vs 备用字段」的文案梯度；
>    备用电话改为「备用联系电话」（6 分）后语义恢复。ground truth 是人工
>    推演的，推演和实现错哪个并不总是一眼可见 —— 让它们对质的正是断言。
>
> **导出面断言**（`Object.keys(fill)` 快照）是 T5.4「不代填 file」的第一道闸：
> 将来谁把写 DOM 的原语引进 fill 层，导出面先红。`file-slot` 分支从第一天
> 就存在于计划里（上传槽位只提示不代填，红线 5）；checkbox（同意条款）
> 不出现在任何分支 —— 用户必须自己勾。
>
> **fixture 以 TS 字符串模块存放**（不是 `.html` 文件）：extension 的
> `types: []` 下 `node:fs` 类型不可用，且 fixture 进 diff 后「改一个标签、
> 期望跟着红」的耦合是显式的。T5.2 的真实站点快照沿用此格式。
>
> 测试数：extension 46 → **76**（fill 30）。全仓 `turbo typecheck test build`
> **9/9 绿**（core 658 / extension 76 / web 43 / evals 11）。
> 变异验证：去掉包含匹配 / 歧义改为填第一个 / 删除 checkbox 排除，三变体全被杀。

### T5.2 前 3 个适配器
- [x] 每个适配器一份快照 fixture + 字段匹配率断言
- [x] 选哪 3 个平台写进 README 并说明选择依据

> **T5.2 交付记录**（`packages/extension/src/fill/platforms.ts` + 三份快照 fixture）：
>
> - **北森 / Moka / 大易**。选择依据：国内校招网申表单层基本被这三家 ATS SaaS
>   覆盖（外企在华校招也多跑在其上），且三家的 DOM 风格恰好是三种典型难度 ——
>   北森（随机 id、span 标题、placeholder 句式，标题对提取层不可见）、
>   Moka（label 规范，适配器刻意少钉，验证「不多钉」的克制）、
>   大易（电话字段打平需适配器解围；年份下拉 vs `YYYY-MM` 走 option-mismatch 不硬猜）。
> - **主判据仍是逐字段 ground truth 整体相等**（每家一份，12 条），不是百分比：
>   fill 的 `key→path:value:source` 与 gap 的 `key→reason:blocking` 双向核对 ——
>   多填、少填、错配、source 标错，任何偏差都红。
> - **版本纪律**（DESIGN 232 的执行点）：`PlatformAdapter.version` 与快照上的
>   `data-platform-version` 钉成相等；CI 冒烟断言每个选择器都在自家快照上解析成功
>   —— 扑空即版本漂移，红灯亮在 CI 而不是用户的浏览器里。
> - **radio 组提取**（T5.1 显式欠账，随本任务还上）：同 name 的 radio 合成一个
>   带 options 的特征，语义与 select 同为「枚举选一」；选项文案与题干分开采集
>   （题干认 aria，选项认 label —— 混了「男/女」会变成「性别/性别」）。
>   fieldset/legend 题干不在最小 DOM 接口内，是已声明的提取缺口。
> - **快照是手工建模的近似（provisional）**，声明与采集流程见 README
>   「适配器与快照维护」；判据不因 provisional 放松，真快照换入后同组断言原样生效。
> - 实现期暴露一个真实目录缺口：`education.0.endDate` 的 kinds 没有 select ——
>   真实站点毕业时间几乎都是下拉，generic fixture 的 `type=month` 建模掩住了它。
> - 测试数：extension 76 → **88**（fill 42）。全仓 `turbo typecheck test build`
>   **9/9 绿**（core 658 / extension 88 / web 43 / evals 11）。
>   变异：错选择器 / 删 radio kind / 废 radio 提取，三变体全被杀。

### T5.3 预览确认与缺口语义
- [x] 任何写入之前必须经过用户确认
- [x] `blocking: true` 的必填项在填充阶段即时提示（否则用户走到提交才发现要回头重填）

> **T5.3 交付记录**（`packages/extension/src/fill/preview.ts` + `apply.ts`，23 断言）：
>
> - **「确认」是一张票，不是一个布尔值。** `createConfirmation` 产出与预览内容
>   摘要（digest）绑定的凭据：digest 只覆盖 fill 条目的 key/path/value/来源/顺序，
>   对每个值、每一项、顺序都敏感；缺口与槽位不进摘要（不可批的东西不占摘要位）。
>   计划在预览之后变过（页面变了、重算过）→ 旧票作废，重新预览。
> - **写入门是运行时 + 编译期双重的。** 从计划到 writer 只有 `applyFillPlan`
>   一条路，路上三道检查：凭据形状正控（不裸 cast）→ 摘要对得上 →
>   批的每一项都在当前计划里（凭据被裁剪/拼过也拒绝）。凭据带模块私有
>   symbol 品牌，裸对象字面量在编译期就造不出来（`@ts-expect-error` 钉住）。
> - **`FillWriter` 是最小平台接缝**（一个 `setValue` 方法）：MV3 内容脚本给
>   真实现，测试给间谍，谁也不 import 谁。门只保证「没确认过的值永远
>   到不了 writer」，writer 落到页面还是落进测试与门无关。
> - **blocking 缺口即时提示**（DESIGN 11.2 例外条款）：`buildPreview` 把
>   blocking 缺口作为一等公民带出（`blockingGaps` + `requiresAttention`），
>   且是 plan 的忠实投影（双向序列化相等断言）。空批准合法 —— 用户看完
>   预览一个都不批，apply 零写入，「一个都不填」被尊重。
> - gap / file-slot / checkbox 没有 approve 的资格：`createConfirmation`
>   对越权 key 直接抛错而不是静默忽略 —— 「允许了不存在的东西」会让门变松。
> - 实现期被自家测试抓到两个测试 bug：digest 前缀断言口径错（分隔符是
>   `\u0002` 非 `:`）；「裁剪凭据」伪造场景选的 key 恰好是合法 fill，
>   改用 gap key 才真正走越权路径。运行期还修了 `declare const brand`
>   不产出运行时值的坑（改 `= Symbol()`）。
> - 测试数：extension 88 → **111**（fill 65）。全仓 `turbo typecheck test build`
>   **9/9 绿**（core 658 / extension 111 / web 43 / evals 11）。
>   变异：拆摘要校验 / 反转 blocking 投影 / 忽略批准集合，三变体全被杀。

### T5.4 上传槽位提示（不代传）
- [x] 断言代码中不存在对 `input[type=file]` 的写入
- [x] 槽位识别用例表：`english|_en|英文` → EN，`chinese|_cn|中文` → CN，`biling|中英|双语` → Bilingual

> **T5.4 交付记录**（`packages/extension/src/fill/upload-slots.ts`，14 断言）：
>
> - **「不代填」是结构性质，不是约定。** 三条证据钉死：① `isFillableKind('file')`
>   为 false，fill 通道对 file 特征关死；② file-only 表单满配批准也零写入
>   （file 特征只能落成 file-slot，而 file-slot 没有批准资格，T5.3 的门）；③
>   raw 源码扫描（vitest `?raw` 导入）钉 `.setValue(` 的调用点只出现在
>   `apply.ts` —— 谁在别处开第二条写 DOM 的路，这里先红。笨判据不漏报。
> - **槽位词汇与 core 交付命名是同一套**：分类结果就是 `ViewSuffix`
>   （EN / CN / Bilingual），建议文件名直接 `deliveryFileName(DEFAULT_FILE_STEM, kind)`
>   ——「看到提示就知道该拖哪个文件」不靠记忆，靠词汇表共享。
>   认不出的槽位 `unrecognized` 且**不给**建议文件名（猜一个比不给更糟）。
> - **用例表逐条落地**（大小写不敏感，key 与标签合成一个信号池）：
>   双语信号压过英/中（一个槽位不会同时是两份）；EN 与 CN 信号并存 =
>   歧义不猜（与填充层 ambiguous 同一立场）；无信号 → unrecognized。
>   已声明缺口：连字符变体（`resume-en`）不在表内 —— 扩表必须先有
>   真实站点槽位样本，不许凭空加模式。
> - `buildUploadNotices` 对 file-slot 与 features 按 key 对账，对不上号
>   （两份数据不是同一次提取的）抛错而不是静默跳过。
> - 测试数：extension 111 → **125**（fill 79）。全仓 `turbo typecheck test
>   build` **9/9 绿**（core 658 / extension 125 / web 43 / evals 11）。
>   变异：双语优先级降位 / 歧义改猜 CN / 建议文件名后缀错位，三变体全被杀。

### T5.5 提交前审核摘要
- [x] 只拦截一次，放行由用户点击
- [x] 摘要区分「自动填的」与「启发式猜的」

> **T5.5 交付记录**（`packages/extension/src/fill/submit-review.ts`，11 断言）：
>
> - **摘要按 `source` 分列，且是投影不是重算**（DESIGN 11.4 关键设计）：
>   adapter 来源进 `autoFilled`（全部来自档案），heuristic 来源进
>   `heuristicGuessed`（请重点核对，UI 高亮）。两列互斥、并集恰好等于
>   全部 fill，双向序列化投影钉死 —— 混列、漏列都红。「仍为空的必填」
>   直接复用 preview 的 `blockingGaps`（T5.3 的口径只有一处），
>   上传槽位与 digest 原样透传。
> - **只拦截一次的执行点**：创建门（`createSubmitGate`）即拦截，每份
>   计划内容一次；`releaseSubmit` 是用户点击在纯逻辑层的化身，一次性
>   消费 —— 第二次调用抛错（那不是「再确认」，是 UI 双重提交 bug）。
>   DESIGN 11.4 的两个反面教材各落一颗钉：拦截后不放行 = 阻碍 →
>   门不带任何自动继续的路径；拦截后静默放行 = 欺骗 → 本模块不存在
>   绕过 `releaseSubmit` 产出合法凭据的路径（品牌 symbol 模块私有 +
>   门由 WeakMap 登记，伪造对象查无此门）。
> - **放行凭据与摘要 digest 绑定**：`verifySubmitRelease` 是消费方
>   （提交事件放行点）的验收 —— 拦截之后页面变过 → digest 对不上 →
>   旧凭据作废，重新拦截。过期凭据与伪造凭据死在同一条路上。
> - 已声明缺口：DESIGN 11.4 摘要第四行「本次新录入」依赖 T5.6 回填
>   闭环的手填捕获数据，不预置占位字段 —— 数据源存在之前先给形状，
>   就是给猜留门。
> - 过程中 typecheck 抓到一个测试抓不到的真问题：`unique symbol`
>   不能索引 `Record<string, unknown>`，形状校验改为类型谓词守卫
>   （`isSubmitReleaseShape`，与 apply.ts 同款）。
> - 测试数：extension 125 → **136**（fill 90）。全仓 `turbo typecheck
>   test build` **9/9 绿**（core 658 / extension 136 / web 43 / evals 11）。
>   变异：分列对调 / 删二次放行抛错 / 删 digest 校验、删形状校验，
>   四变体全被杀。

### T5.6 回填闭环
- [x] 手填新字段 → 询问 → 写回档案并重建索引
- [x] 与已有值冲突时不自动覆盖，交给用户选

> **T5.6 交付记录**（`packages/extension/src/fill/backfill.ts`，18 断言）：
>
> - **三段式，检测与写回之间没有直达路径**：`detectBackfillCandidates`
>   （对比提交时刻表单值与计划：改了已填字段 edited / 填了缺口字段
>   manual）→ `buildBackfillProposal`（按档案现值分诊 new / conflict，
>   恰好相同 = same 无信息量不进 proposal）→ `applyBackfill`（只写
>   decisions 里显式点头的条目）。空决策 = 档案逐字节不变 ——
>   「不静默保存」是结构，不是习惯。
> - **「冲突不自动覆盖」的另一半**：conflict 条目没有决策 = 保留档案
>   值，keep 是缺省不是 replace；展示两个值是 UI 的事，「选了才覆盖」
>   在纯逻辑层就是「决策列表里没有它就不写」。
> - **outbid / ambiguous 缺口不回填**（测试逼出的语义边界）：这两种
>   缺口的 path 归属已被计划判为不可信 —— 「紧急联系人电话」被词汇表
>   误配上 `basics.contact.phone`，手填值回填进 phone 就是污染档案。
>   「填错比不填更糟」的回填方向镜像是**存错比不存更糟**。no-catalog-match
>   → customFields（schema 唯一开放槽位，级别策略默认 B 级），靠
>   「询问 + 显式批准」把关；验证码这类一次性字段用户硬要批准是用户
>   的选择 —— 已声明权衡，不加「判断字段值不值得存」的启发式。
> - **写回白名单与目录对称**：`BACKFILL_PATHS` 与 `FILL_CATALOG` 的
>   path 集合由断言钉死双向相等 —— 目录加了条目忘了写回通道（或
>   反向冒出平行词汇表），红灯亮在回填测试里，而不是亮在用户「填了
>   却不记住」的体验里。父容器缺失（`education` 为空时的
>   `education.0.endDate`）抛错并把话说全：该补的是整条教育经历。
> - **指纹绑定检测时刻的档案**（`serializeArchive`，同步纯函数）：
>   apply 时重新比对，检测之后 web 端改过档案 → 旧 proposal 作废。
>   与 T5.3 确认凭据、T5.5 放行凭据同一套机制，只是这里的「世界」
>   是档案而不是页面。决策 id 越权（不在 proposal 里）抛错不静默忽略。
> - **「重建索引」的可执行判定**：写回后的档案用 `FILL_CATALOG` 的
>   `read` 能读回页面值 —— 下一次 `buildFillPlan` 自动填上它。产出
>   必过严格 schema round-trip（`deserialize(serialize(draft))`）：
>   手填值写不进档案形状（日期格式、薪资「25k」）在这里炸，而不是
>   等保存密文时才炸。
> - 测试数：extension 136 → **154**（fill 108）。全仓 `turbo typecheck
>   test build` **9/9 绿**（core 658 / extension 154 / web 43 / evals 11）。
>   变异：忽略决策全写入 / 删 outbid 排除 / 删指纹校验 / 越权静默忽略，
>   四变体全被杀。typecheck 两次抓到测试没暴露的真问题
>   （`basics.location` 可选、突变误插 fill 分支）。
> - 平台接缝（留给确认 UI）：拿到 `pageValues`（提交时刻表单值）→
>   detect → proposal 交用户勾选 → decisions → `applyBackfill` →
>   `Vault.saveArchive`（payload 换、DEK 不动，DESIGN 11.3 增量录入）。

---

## M6 · 交付

- [ ] T6.1 `README.md`：以第 7 节的七句话开头 + 已知限制（尤其"忘记口令数据就没了"）
- [ ] T6.2 在线 Demo（静态站部署，点开链接即可看）
- [ ] T6.3 `CONTRIBUTING.md` + 术语表贡献指南（`knowledge/` 是唯一对社区开放的模块）
- [ ] T6.4 隐私自证说明：打开 DevTools Network 面板的验证步骤（配图或 GIF）

---

## 不需要做的事

- 不做服务端、不做账号体系、不做跨设备同步
- 不写适配器以外的爬虫、不做批量投递
- 不做向量库、不做多模型 A/B 面板、不做可视化编排界面

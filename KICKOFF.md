# 启动指令（KICKOFF.md）

> 这份文件是给**执行任务的那个 Agent** 的开场话术模板。每次开一个新会话，复制一段，改掉任务编号即可。
> 一个任务 = 一个会话。不要在一个会话里跨里程碑连着做。

---

## 一、给哪些文件

| 文件 | 作用 | 什么时候读 |
|---|---|---|
| `AGENTS.md` | 作业约定、7 条红线、命令、禁止引入清单 | **每次会话开头必读全文** |
| `TASKS.md` | 任务切分与**可执行验收标准** | 只读本次任务那一条，不要通读 |
| `DESIGN.md` | 设计依据（为什么这么做、否决了什么） | 需要判断时按章节查，不要通读 |

**只给 `DESIGN.md` 是错的。** 它只有理由、没有边界，Agent 会在"不做服务端""数字可溯源""适配器优先"这些地方自由发挥。

---

## 二、单任务开场话术（直接复制，替换 `<...>`）

```
项目路径：<仓库绝对路径>

先读 AGENTS.md 全文，再读 TASKS.md 中的 T<编号>，然后按需查 DESIGN.md 的 <章节>。
本次只做 T<编号> 这一件事，不要顺手做 TASKS.md 里的其他任务。

规则：
1. 先写断言（测试），再写实现；验收标准逐条落实，不许用「已实现」代替证据。
2. 红线见 AGENTS.md 第 5 节 —— 任何一条都不要碰。
3. 不要引入 AGENTS.md 第 2 节「明确不允许引入」清单里的任何依赖。
4. 如需推翻某条 ADR：停下来告诉我，说明原否决理由为什么不成立。不要自己决定。
5. 完成后报告：改了哪些文件 / 新增了哪些断言 / 每条验收标准你是怎么验证的。
```

## 三、验收话术（做完之后再说一遍）

```
逐条核对 TASKS.md 中 T<编号> 的验收标准，每条标记为 通过 / 未通过 / 无法验证，并附证据（测试名或命令输出）。
没有证据的一律算未通过。
```

---

## 四、Agent 跑偏的信号

| 现象 | 说明什么 | 怎么修 |
|---|---|---|
| 开始"顺便"重构别的模块 | 任务边界没给死 | 重申「只做 T\<编号\>」 |
| 引入了向量库 / Agent 框架 / 服务端 | 红线没读进去 | 让它先复述 AGENTS.md 第 5 节 |
| 说"已实现，测试通过"但不贴测试名 | 验收话术没给 | 补第三节那段话 |
| 断言写成"应该能正常工作" | 你的验收标准不够具体 | 回头改 `TASKS.md` 那条 |
| 悄悄改了 ADR 背后的行为 | 上下文太长，前文约束失效 | 换新会话，任务再切细一点 |

---

## 五、Agent 替不了你的四件事

1. **拍板 ADR** —— 面试时被问"为什么不用 Playwright / 为什么不建向量库"，只有你答得出来。这是这个作品集最值钱的部分。
2. **真机验证网申填充** —— 需要真实账号和真站点，只能你自己点。
3. **术语表的中文专业度** —— 中英职位名映射（"算法工程师" ≠ Arithmetic Engineer）需要人来判。
4. **决定要不要继续投入** —— 这条我替你决定不了。

---

## 六、会话顺序

严格按 `TASKS.md` §0 的竖切走：

```
会话 1  T1.1  schema
会话 2  T1.2  加密
会话 3  T1.3  + T1.4（密文库 + B 级不出网测试）
会话 4  T2.1  编译层
会话 5  T2.2  匹配引擎
会话 6  T3.2  硬校验闸门
会话 7  T4a  渲染与 PDF 导出
```

走完这七步你就有一个**能跑的东西**加**三处可演示的亮点**。之后再按 M4/M5/M6 展开。

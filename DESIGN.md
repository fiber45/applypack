# 简历智能生成与网申自动填充平台 · 技术方案

> 面向开源发布与作品集展示，基于一轮完整的设计质问（grilling）收敛而成。
> 所有决策均带「被否决的方案 + 否决理由」，便于后续复盘。
>
> **文档分工**：本文件 = 为什么这么做（设计依据）｜`AGENTS.md` = 怎么做（作业约定、红线、命令）｜`TASKS.md` = 做什么（按里程碑切分、每条带可执行验收标准）。
> 交给任何执行者时，三份一起给；只给本文件，等于只给了理由没给边界。

---

## 0. 定位

输入：用户上传的全部个人信息（经历、技能、教育背景）+ 目标岗位 JD。
输出：① 按 JD 自动筛选并润色到专业水平的简历；② 网申表单的自动填充。

**不是** SaaS，是**本地优先（local-first）的开源工具**。共享的是软件，不是数据。

---

## 1. 核心约束与架构推论

### 1.1 需求内部的真实矛盾

> 「后端被攻破也拿不到明文」要求服务端**在数学上无法解密**；
> 「按 JD 筛选 + 润色」要求总得有东西**能读懂明文**。

这两条无法用同一套信任模型同时满足。**解法不是加密得更狠，而是取消服务端。**

### 1.2 由此得到的架构

| 推论 | 说明 |
|---|---|
| **零后端** | 无服务端 ⇒ 无多租户 ⇒ 无多租户隔离问题。隔离由「各自独立部署」天然保证 |
| **隐私叙事升级** | 从「架构承诺」变为「可验证事实」：打开 DevTools Network 面板，全程无任何请求携带简历内容 |
| **威胁模型转移** | 「服务端被攻破」退役，取而代之的是**扩展权限滥用**与**供应链投毒** |
| **代价认领** | 忘口令即丢数据；无跨设备同步；搜索/缓存/批处理全在客户端扛 |

### 1.3 关键的隐私分界线（不可越过）

| 分级 | 内容 | 规则 |
|---|---|---|
| **A 级 · 可交给 LLM** | 经历、项目、技能、教育背景、成果数字 | 生成简历所必需，允许组进模型请求 |
| **B 级 · 永不出端** | 身份证号、手机号、住址、紧急联系人、照片、健康与家庭信息 | 仅用于本地填充；代码层禁止进入任何模型请求 |

**判据（比上面那个清单更重要）**：A / B 的分界不看「敏感与否」，看**模型是否需要知道**。
- **A 级 = 能力与经历叙事**（你会什么）—— 模型必须读到才能改写
- **B 级 = 身份与联系标识**（你是谁）—— 模型写一条 bullet 时完全不需要它；
  产出物中这部分内容由**确定性代码注入**（渲染是第 7 步，纯确定性），绝不经过模型

这条判据解开了清单里一个容易困惑的地方：**手机号明明要印在简历上，为什么是 B 级？**
因为「上简历」和「进模型请求」是两件事，**B 级管的是后者**。

按此判据，清单之外还有三个字段被划入 B 级（`T1.1` 实现时判定，待确认）：
`basics.name`（姓名）、`basics.contact.email`、`customFields`（语义未知的字段保守默认 B）。

实现：schema 上的 `neverSendToLLM: true` 标记 + 请求组装层的强制断言。
**LLM 厂商是第三方，其留存策略不受你控制 —— 这条线缺了，"注重隐私"就是空话。**

---

## 2. 技术选型总览

| 层 | 选型 | 说明 |
|---|---|---|
| 包管理 | pnpm workspace + Turborepo | monorepo，core 被两端共享 |
| 核心库 | 纯 TypeScript，零框架依赖 | 可独立测试、可被两端复用 |
| Web 端 | Vite + React + Tailwind | 静态产物，部署 GitHub Pages / Cloudflare Pages |
| 扩展端 | **WXT** | MV3 生命周期坑太多，不自研脚手架 |
| 数据 Schema | **JSON Resume** + 中文 ATS 字段扩展包 | 白拿生态，扩展包可反哺上游 |
| 加密 | **libsodium-wrappers**（WASM）+ **hash-wasm**（Argon2id） | 永不自己实现密码学；AEAD 用 **XChaCha20-Poly1305**（见 ADR-9） |
| 本地存储 | IndexedDB（Web 端）/ chrome.storage.local（扩展端） | 均只存密文 |
| LLM 接入 | **Vercel AI SDK** | 厂商抽象 + 流式；**不引入 Agent 框架** |
| 检索 | **MiniSearch**（BM25） | 范例池仅数百条，上向量库是负分 |
| PDF 渲染 | react-pdf / 打印 CSS | 排版引擎不自研，模板设计自研 |
| 文本提取 | pdf.js + mammoth | 只负责取文本，结构化交给 LLM |
| 评测 | **自研**（JSON 用例 + 确定性断言 + CI） | 成本低、含金量高 |
| 浏览器自动化 | **不使用** | 见 ADR-6 |

---

## 3. 架构与关键模块

### 3.1 三端结构

```
resume-copilot/
├─ packages/
│  ├─ core/                  # 纯 TS，无 UI / 无框架依赖
│  │  ├─ schema/             # JSON Resume + 中文 ATS 扩展字段
│  │  ├─ crypto/             # Argon2id → KEK → DEK → XChaCha20-Poly1305
│  │  ├─ vault/              # 密文库读写（存储层可替换）
│  │  ├─ compile/            # LLM 编译器：经历 / JD → 结构化
│  │  ├─ matching/           # 打分引擎：加权规则 + 理由生成
│  │  ├─ polish/             # 润色 + 硬校验（幻觉数字检测）
│  │  ├─ pipeline/           # 七步管线编排
│  │  ├─ llm/                # AI SDK 封装 + Prompt 版本管理
│  │  └─ eval/               # 评测用例与断言
│  ├─ web/                   # 静态 SPA：档案编辑 / 生成 / 预览 / 导出
│  └─ extension/             # WXT：内容脚本 + 适配器 + 确认 UI
├─ fixtures/                 # 脱敏评测集
└─ .github/workflows/        # CI：eval 作为合并关卡
```

**两端桥接**：扩展声明 `externally_connectable` 限定为自有域名；Web 端保存后把**密文**推送给扩展。扩展不复制密钥推导逻辑之外的任何东西。

### 3.2 密钥与解锁

```
口令 ──Argon2id(独立盐, m=64MiB/t=3/p=1)──► KEK ──封装──► DEK ──XChaCha20-Poly1305──► 密文记录
```

- 密文副本同时存在于 Web 端 origin 与扩展 storage
- **会话解锁**：每会话解锁一次，15 分钟无操作自动清除内存中的明文
- **恢复路径只有一条，且方向向前**：`.vault` 备份 + 换口令。无服务端 ⇒ 无口令重置，这是必须写进 README 的已知限制
- **没有恢复码，也没有任何其它后门**：解密入口只有一个 —— 口令。见下方 ADR-10 的说明

**关于「恢复码」—— 一次自我否决。** 本文档早先版本在这里写的是「`.vault` 文件 + 恢复码」，
与第 7 节第 5 句「忘记口令数据就没了」直接冲突：只要存在一把能绕开口令打开档案的钥匙，
那句话就是假的。两者必须删一个，删掉的是恢复码。理由不是「实现麻烦」，是它**解决错了问题**：

- 用户真正焦虑的不是「我忘了口令」，而是「我怕我将来会忘」。这一点用换口令（`changePassphrase`）就解决了 ——
  趁还记得时换成更好记的，也可以把导出的备份用另一把口令重封。这是**向前**的路径，代价为零。
- 恢复码则要求用户额外长期保管第二把钥匙。而实践中它会被写进备忘录、截屏存进相册 ——
  一个与口令同处一室、却通常更短且从不更换的秘密。**它降低的是使用门槛，不是丢失风险。**
- 它还顺手摧毁了一句最有价值的承诺。「忘记口令数据就没了」是一句用户听得懂、能自己验证、
  且听起来就不像营销的话；「忘记口令**且丢失恢复码**才没了」则把这句承诺降级成一段免责声明。

代价认领：**没有任何恢复码，忘记口令 + 手上没有备份 = 数据永久丢失。**
这条写进 README 第 5 句与 `T6.1` 的已知限制。

**KDF 参数与取值依据。** 单独列表的原因：KDF 参数是整个系统里**唯一一处「调小一点就悄悄变弱、且没有任何功能会坏掉」**的地方。没有依据，它迟早会被「为了让测试跑快点」改成 8 MiB。

| 参数 | 取值 | 依据 |
|---|---|---|
| 算法 | Argon2id | PHC 竞赛优胜者、RFC 9106 标准；内存硬，抗 GPU/ASIC 远强于 PBKDF2 |
| 内存 m | 64 MiB | 高于 OWASP 的两档推荐（19 MiB / 47 MiB），对齐 RFC 9106 的内存受限档位。再往上（如 256 MiB）会让低端手机解锁卡顿 —— 而卡顿会促使**用户干脆不设口令**，那是比参数弱更糟的结果 |
| 迭代 t | 3 | 与 RFC 9106 内存受限档一致，强于 libsodium `crypto_pwhash` 的 INTERACTIVE 档（t=2） |
| 并行度 p | 1 | OWASP 各推荐档均为 1；p>1 抬高低端移动端的内存峰值与调度差异，浏览器场景收益低 |
| 盐 | 16 字节，每条档案独立随机 | 同一口令在不同档案下派生出不同 KEK，一条泄漏不波及另一条 |
| AEAD | XChaCha20-Poly1305 | 见 ADR-9 |

实测耗时：本机 Node 22 约 250–350 ms，浏览器端同量级。

**信封里的 KDF 参数必须按不可信输入校验。** 参数随文件走（自包含是刻意的），也就意味着它可以被任意构造。一个 `memoryKiB: 4194304` 的 `.vault` 文件能让浏览器在用户点「导入」那一刻尝试分配 4 GiB 内存。因此有硬上限：内存 ≤ 256 MiB、t ≤ 64、p ≤ 16，越界一律 `invalid_params`。

### 3.3 AI 管线（七步）

| # | 步骤 | 类型 | 职责 |
|---|---|---|---|
| 1 | 录入编译 | LLM | 自由文本 / 旧简历 → 结构化经历 |
| 2 | JD 编译 | LLM | 抽取硬门槛、技能、关键词表、语域 |
| 3 | 匹配打分 | **确定性** | 加权排序 + 逐条生成选中/淘汰理由 |
| 4 | 澄清 Agent | LLM（循环） | 识别缺量化信息的条目，精准追问用户 |
| 5 | 润色改写 | LLM | 只动语言层，不新增事实 |
| 6 | 硬校验 | **确定性** | 幻觉数字 / 关键词覆盖 / 长度 / 动词重复 |
| 7 | 渲染导出 | 确定性 | 模板 → PDF / 网页预览 |

**步骤 6 是整份方案的工程质量核心**：LLM 输出的**每一个数字都必须在原文中可溯源**，任何新出现的数字直接拒绝并回炉。这把「润色」从"看起来像那么回事"升级为"可验证地没有撒谎"。

**RAG 的正确位置**：只服务「怎么说」，不服务「说什么」。
检索对象是外部的改写范例池（few-shot）、行业动词与量化模板、JD 黑话词典。
**绝不索引用户数据** —— 几十条经历全量进 context 即可，向量召回反而会漏掉恰好最相关的那条。

### 3.4 填充引擎

```
内容脚本 → 页面特征提取(label/aria-label/name/placeholder)
        → 适配器注册表（命中已知平台则用精确选择器）
        → 启发式匹配（长尾站点，中文同义词表 + 字段类型推断）
        → 预览确认 UI（用户逐项确认后才写入 DOM）
```

**强制确认不是体验妥协，是正确性保障**：填错一个字段（如把「学历性质」填成「学位」）可能直接毁掉一次网申 —— 填错比不填更糟。

---

## 4. 决策记录（ADR）

| # | 决策 | 被否决的方案 | 理由 |
|---|---|---|---|
| 1 | **零后端 local-first** | 公有 SaaS + 列级加密 | 只有取消服务端才能让隐私从"承诺"变成"可自证的事实"；隔离亦无需实现 |
| 2 | **双载体：Web + 扩展** | 扩展即应用 / 桌面 Tauri | Web 提供可分享的链接（作品集刚需），扩展是自动填充的唯一合法载体；Tauri 增加安装门槛 |
| 3 | **扩展持密文副本** | 实时向 Web 端取数 | 网申发生在标签页中，不应依赖另一标签页恰好开着且已解锁 |
| 4 | **LLM 编译 + 确定性匹配** | LLM 逐条判相关性 | 可复现、可解释、零边际成本；能回答"凭什么删我这段" |
| 5 | **不对用户数据做 RAG** | 向量库检索经历 | 数据量小、负收益、召回不稳 |
| 6 | **不使用无头浏览器自动化** | Playwright 服务端代填 | 需服务端跑浏览器 + 需用户凭据 + 体积巨大 + 易被反自动化识别；「用户自己浏览器 + 主动触发」与「服务器代替操作」性质完全不同 |
| 7 | **适配器优先 + 强制确认** | 纯启发式 / LLM 现场猜字段 | 兼顾准确率与覆盖率；LLM 猜字段需发送页面结构，可能与 B 级数据不出端冲突 |
| 8 | **档案 schema 用严格模式**（未知字段报错） | `.passthrough()` 保留未知字段 ／ 默认 strip 静默丢弃 | 未知字段**没有级别**，因而无法参与「B 级不出端」判定 —— 一个混进来的 `contacts_backup` 就能绕过整条红线。passthrough 会把无级别的数据留在档案里，strip 会静默丢用户数据，两者都不可接受 |
| 9 | **AEAD 用 XChaCha20-Poly1305** | AES-256-GCM（原选型）／改用 WebCrypto 实现 AES-GCM | 实现时实测：**libsodium.js 的 WASM 构建（含 `libsodium-wrappers-sumo`）不含 AES-256-GCM** —— `crypto_aead_aes256gcm_is_available` 在标准版里不存在、在 sumo 版里是 `undefined`。不是配置问题，是 wasm 没有 AES-NI、上游选择不编译。要用 AES-GCM 只能改走 WebCrypto，那要求 `core` 放弃「零全局依赖」、由 Web 端与扩展端各自注入原语实现 —— 多一整层间接，换来的只是「算法名字更眼熟」 |

| 10 | **AAD 的作用域按「影响密钥」与「影响解释」切开** | 用一条 AAD 串绑定全部头部字段（T1.2 的初版实现如此）／payload 不做 AAD | 换口令必然换盐（盐逐次随机是本设计的前提）。若 payload 的 AAD 含盐，换口令就等于让内容**永久解不开** ——「换口令只重封 DEK」当场失效，两层密钥设计白做。故：`wrappedKey` 的 AAD 绑定整个头部（含盐与 KDF 参数），`payload` 的 AAD 只绑定「如何解释这份密文」（format / version / cipher.algorithm），**不含盐**。这不让内容变弱：KDF 参数已通过「KEK 不对 ⇒ DEK 解不出 ⇒ 内容也解不开」那条链被间接绑定，再绑一次只是把内容钉死在某一代口令上 |

ADR-8 的代价要认领：**严格模式使「任何形状变更必须 bump `schemaVersion` 并写迁移函数」成为硬错误。**
这是有意的 —— 它让「忘了写迁移」不可能被忽略，而不是靠约定。

**ADR-9 的补充说明**：这个决策与最初的选型相反，所以要说清「为什么换掉的不只是可选项、而是更坏的那个」。
XChaCha20-Poly1305 的 192-bit nonce 允许**随机生成 nonce 而无需计数器**；AES-GCM 只有 96-bit，
同一密钥下随机使用约 2^32 条记录后就有碰撞风险 —— 而 **nonce 复用对 GCM 是灾难性的**（泄漏明文异或，且认证密钥可被恢复）。
选一个「随机就安全」的算法，等于消掉了一整类事故。代价是偏离 NIST 标准算法的合规叙事
（Poly1305 是 RFC 8439，XChaCha20 是 libsodium 的 IETF 草案扩展，TLS 1.3 里已是标准套件），对个人求职工具可接受。

**这次实测还留下一条更一般的教训**：技术选型表里写下的**具体算法名**，在写下的那一刻只是假设。
「用 libsodium」是决定，「用 AES-GCM」是决定之上的推论 —— 推论需要被验证，而验证的时机是写第一行实现代码时，不是上线前。

**ADR-10 是怎么被发现的。** 不是靠 review，是靠写 T1.3 的第一行实现代码 ——
与 ADR-9 同一种发现方式，这已经是第二次了。当初写头部绑定串时把盐放进去，
动机只是「反正都绑上更安全」，看不出任何问题；直到要实现「换口令」这一步，
那条 AAD 才暴露出来：它会把 payload 钉死在旧盐上。

教训与 ADR-9 同源，但更具体：**「绑得越多越安全」在 AAD 上不成立。**
AAD 的正确范围是「解释这团密文所必需的上下文」，不是「所有能绑上的元数据」。
多绑的部分不会让攻击者更难，只会把合法的变更（换口令、升级 KDF 参数、
调整迭代次数）变成数据损坏 —— 而这种故障只在用户真的去换口令时才炸，
常规的单测覆盖不到。

**同一个模式已经出现两次了**：一个「看起来更安全」的直觉，代价是让某个正常功能
变得不可实现。所以往后判断这类取舍时，要问的不是「加了它会不会更安全」，
而是 **「加了它之后，哪些合法操作会变成不可能」** —— 后一个问题有确定答案，前一个没有。

**红线**：永远不做「批量一键投递」。加了它，项目会从"求职辅助工具"变成"自动投递爬虫"。

---

## 5. 已知限制与风险

| 风险 | 缓解 |
|---|---|
| 忘口令 = 数据永久丢失 | 加密备份导出（`.vault`）＋ 趁记得时换口令；README 显式声明。**刻意不提供恢复码**，理由见 3.2 |
| 无跨设备同步 | `.vault` 导入导出；未来可接用户自选的 WebDAV / 网盘 |
| 扩展权限敏感（可读写网申页面 DOM） | 权限最小化、MV3 无远程代码、可复现构建、单一发布者签名 |
| 供应链投毒（静态站被篡改） | 固定构建产物哈希、GitHub Actions 可复现构建、披露构建流程 |
| 适配器随平台改版失效 | 选择器版本化 + CI 冒烟检测 + 快速降级到启发式 |
| 长尾站点准确率有限 | 强制预览确认 |
| 中文 ATS 字段语义混乱 | 维护术语表（学历 / 学位 / 学历性质三分；毕业时间 vs 毕业年月） |

---

## 6. 里程碑

1. **M1** core 骨架：schema + 加密 + 密文库（可验证"存得进、别人读不出"）
2. **M2** 编译器 + 匹配引擎 + 理由展示 UI
3. **M3** 润色 + 硬校验 + 评测集接入 CI
4. **M4** 模板渲染与 PDF 导出 + 三版投递包（EN / CN / 双语顺序拼页）+ ATS 文本层可提取性校验
5. **M5** 扩展：内容脚本 + 3 个适配器 + 上传槽位提示 + 确认交互
6. **M6** README、在线 Demo、贡献指南

---

## 7. README 开头最值钱的七句话

1. 你的简历数据从不离开浏览器 —— 打开 Network 面板，你可以自己验证。
2. 筛选是全确定性的，你能看到每一段经历**为什么**被选中或淘汰。
3. 润色层带硬校验：任何原文里找不到的数字，都会被拒绝并重写。
4. 填表用你自己的浏览器、由你逐项确认；服务端不代替你做任何操作。
5. 忘记口令数据就没了 —— 这是真端到端加密的代价，我们选择接受。
6. 中英双版简历与自我介绍出自同一份档案，数字集合必须完全相等 —— 我们机械校验这一点。
7. **本项目不代为提交。** 提交前你会看到一份逐项摘要，确认之后由你自己点下提交按钮。

---

## 8. 大模型怎么用 —— 编排规格

> 本节回答「怎么用」，不是「用哪个」。核心命题：**用模型的方式，比用哪个模型重要一个数量级。**

**第一原则**：一次调用出不了专业简历。在同一个 prompt 里同时要求「挑经历 + 改写 + 保住事实 + 控长度 + 对齐关键词」，模型必然顾此失彼 —— 因为它手上没有校验器。
**解法：把「生成」与「验收」分开，且验收不交给模型。**

### 8.1 上下文分层与缓存前缀

从稳定到易变排列，缓存断点打在档案之后：

```
[系统提示：角色 + 硬约束 + 输出 schema]   永不变   ─┐
[术语表 + 改写范例 few-shot]              极少变    ├─ 缓存
[用户 A 级档案（结构化 JSON）]             会话内不变 ─┘
──────── cache breakpoint ────────
[结构化 JD]                               每次变
[本轮任务指令]                             每次变
```

把 JD 摆在档案之前是常见错误，会让缓存全部失效 —— 这是零后端架构里**唯一免费的优化**，摆错直接贵约十倍。

### 8.2 上下文裁剪：必须保留未经改写的原文

每条经历只带四项：名称、时间、**原始文本（一字不改）**、匹配分数与命中关键词。
不带：B 级字段、历史版本、JD 原文（用结构化摘要代替）。

**绝不「先总结再改写」** —— 原始数字一旦在预处理阶段丢失，幻觉校验就永远失效。

### 8.3 结构化输出用 schema，不用自然语言要求

编译层与改写层强制 JSON Schema / strict mode（自然语言要求约 3–5% 返回坏 JSON）。
附带收益：schema 本身就是设计文档。

```ts
{ bullets: [{ text, sourceSpan, evidence[], keywordsHit[] }] }
```

看到 `evidence` 与 `sourceSpan`，任何人立刻明白这个系统在做事实溯源。**schema 设计比 prompt 文案更能决定输出质量。**

### 8.4 改写逐条并发，不批量

每条 bullet 一次调用、并发发出：总延迟 ≈ 最慢一条，缓存前缀抵消重复上下文。
收益：**失败隔离** —— 单条编造数字只重试该条，不必整份回炉。
副作用与解法：风格漂移 → 把**已定稿的前 3 条作为风格锚点**带进后续调用。

### 8.5 Agent 的反馈粒度决定它能否收敛

闸门返回结构化失败对象，而不是模糊评价：

```ts
{ pass: false, failures: [
  { bulletId: "b7", reason: "number_not_in_source", offending: "提升 40%" } ]}
```

拿到这种反馈，修复成功率极高；拿到「写得不够好」，模型只会换个说法再编一次。

### 8.6 把「专业水平」翻译成可度量的目标函数

| 指标 | 阈值 | 性质 |
|---|---|---|
| 数字可溯源 | 100% | **一票否决** |
| 强动词开头 | 100% | 硬约束 |
| 被动语态出现 | 0 | 硬约束 |
| 单条长度 | ≤ 45 字 / ≤ 2 行 | 硬约束 |
| 同一动词重复 | ≤ 2 次 | 硬约束 |
| 量化率（含数字条目占比） | ≥ 70% | 优化目标 |
| JD 关键词覆盖 | ≥ 60% | 优化目标 |

**不是在 prompt 里描述「专业」，而是在代码里定义「专业」。**
有了这份清单，「优化」就从形容词变成了 agent 每轮都能拿到梯度反馈的目标函数。

### 8.7 三段式总览

| 段 | 是否需要模型 | 职责 |
|---|---|---|
| 前置编译 | 是（一次性，structured output） | 经历 / JD → 结构化字段 |
| 中间循环 | 是（Agent + 白名单工具 + 确定性闸门） | 唯一需要判断力的地方 |
| 后置生成 | **否** | 校验、渲染、导出 |

模型只出现在第 1、2 段，且第 2 段里它每一步都有外部反馈。

---

## 9. 简历优化 Agent 规格

**定位**：有界工具循环（bounded tool loop），不是对话式助手。

- **输入**：A 级档案（已剥离 B 级字段）+ 结构化 JD + 目标页数 + 已有草稿
- **状态**：逐条 bullet 的候选/定稿 + 未解决问题清单 + 轮次计数 + token 预算
- **输出**：定稿 bullets + 每条选择理由 + 未采纳项及原因 + 向用户提出的追问

**工具白名单（编译期固定，纯函数、零网络）**

| 工具 | 作用 |
|---|---|
| `score_match` | 调确定性匹配引擎，返回分数与分项明细 |
| `keyword_gap` | JD 关键词命中与缺口 |
| `rewrite` | 调 LLM 改写单条 bullet |
| `verify_facts` | 幻觉溯源：数字与实体比对原文 |
| `ask_user` | 暂停循环，向用户追问缺失信息 |

**三道缰绳**

1. **白名单**：Agent 不能发起任意网络请求，不能读取 B 级字段
2. **确定性验收**：唯一的完成判定是闸门，**验收器不许是 LLM** —— 这是防止「两个 LLM 互相吹捧」的唯一办法
3. **硬性终止**：轮数上限（默认 6）+ token 预算 + 超时；超限返回当前最好版本并标记「未完全达标」

**可测试性**：工具是纯函数、验收是确定性的 ⇒ 同一 `(profile, JD)` 的**通过与否**应高度一致（措辞可不同）。这让它可以用评测集跑 CI —— 也是它配得上「Agent」而不是「prompt 堆砌」的证明。

**与 3.3 节的关系**：本节细化了管线第 3–6 步，它们从独立节点内聚为 Agent 的工具与闸门。

---

## 附录 A. 模型接入与 CORS 约束

**零后端 ≠ 零第三方后端。** 对不支持浏览器直连的厂商，由用户一键部署自运维的 Cloudflare Worker 做中继 —— 用户自己就是可信方，信任模型不被破坏；同时 API Key 存在 Worker secret 而非浏览器。

| 厂商 | 浏览器直连 | 备注 |
|---|---|---|
| Google Gemini | ✅ 原生支持 | 零配置门槛最低 |
| Anthropic | ⚠️ 需 `anthropic-dangerous-direct-browser-access: true` | ZDR 组织明确不支持 CORS |
| OpenAI | ❌ 拒绝带 Web Origin 的请求 | 必须中转 |
| DeepSeek / 通义 / 智谱 | ❓ 社区说法冲突 | **必须实测，结论不可写死** |

**默认单模型**，把调用收敛到 `core/llm/`，节点级模型覆盖只留配置位、不做 UI。
**上线前实测每家 CORS 并把结果写进 README** —— 这张表本身就是严谨性的体现。

成本量级参考（单次完整优化，输入 3–6 万 / 输出 1–2 万 token）：便宜档约 ¥0.15–0.35，中档约 ¥1.9，顶级档约 ¥4.4。绝对值都不大 ⇒ **不该为省钱牺牲质量**，真正该做的是把 token 大头交给便宜模型。

---

## 10. 多语言视图：英文简历与中英自我介绍

### 10.1 核心判断：英文简历不是翻译，是按目标语规范重写

中文简历中的政治面貌、籍贯、身高、照片、期望薪资、婚姻状况，在英文简历语境下要么不存在，要么属于**反歧视敏感项**（英美 ATS 明确不建议出现照片、年龄、性别、婚育信息）。因此**不能走「翻译」路径**。

**抽象**：把「简历」从单一产物提升为 **target**，同一份 A 级档案渲染出多个视图。

| target | 规范约束集（constraint profile） |
|---|---|
| `resume_zh` | 国内网申习惯：可含政治面貌、籍贯；一页 / 两页 |
| `resume_en` | 无照片、无年龄性别、**不用第一人称代词**、强动词开头、严格逆时序、≤1 页（经验 <10 年）、不写 "References available upon request" |
| `resume_en@mnc_campus` | 外企校招专用（见 10.5）：严格 1 页、教育前置、单栏可机读 |
| `resume_zh@mnc_campus` | 同一档案的中文视图，与英版事实对齐 |
| `resume_bilingual@mnc_campus` | 顺序拼页（EN 在前），**非并排** |
| `intro_zh` | 分档：60 秒口播 / 3 分钟口播 / 200 字书面 / 500 字书面 |
| `intro_en` | 同上分档；口播版额外约束**可朗读性**（短句优先，避免书面长从句） |

**关键：自我介绍不是另写一份文档，是同一份档案的另一个视图。** 否则用户改完简历还要回头逐句改自我介绍，迟早对不上。

### 10.2 跨语言一致性校验（可机械验证）

中英两版必须事实一致，但不靠模型自觉：

- 抽取两版的**数字集合**（时间、金额、百分比、规模、名次），要求**完全相等**
- 职位名 / 公司名 / 学校名一一对应
- 条目数量与顺序一致

**任何不一致直接判定失败并回炉。** 这条校验不需要模型，纯字符串与数字比对，属一票否决。

### 10.3 术语表是核心资产

中文职位名与行业术语不可机翻（"算法工程师" ≠ "Arithmetic Engineer"）。需维护**中英职位 / 技能 / 行业术语映射表 + 英文简历动词表**，被三处复用：`resume_en` 渲染、`intro_en` 生成、`core/llm` 的 few-shot 检索。

术语表属**外部知识（非用户数据）**，可随项目分发、可由社区贡献 —— 最适合作品集里的 PR 友好贡献点。

### 10.4 schema 影响

JSON Resume 是单语言的，需加 `i18n` 扩展：

```ts
{ title: { zh: "算法工程师", en: "Algorithm Engineer" }, ... }
```

---

## 11. 信息缺口闭环

### 11.1 统一抽象

生成侧的 `ask_user` 与填充侧的缺失提示**不是两套机制**，而是同一份缺口数据的两种呈现：

```ts
type MissingField = {
  path: string        // 档案中的字段路径
  reason: 'absent' | 'ambiguous' | 'quantification_missing'
  blocking: boolean   // 是否阻塞提交
  origin: 'generation' | 'autofill'
}
```

### 11.2 三种呈现，共用一份缺口数据

| 场景 | 策略 | 理由 |
|---|---|---|
| 生成简历时 | 循环内追问，**仅「缺量化」**，通常 2–3 次 | 每次都是用户答得上的具体问题 |
| 网申填充时 | **聚合提示，绝不在填写过程中打断** | 打断会摧毁体验，缺口放进摘要一次性给出 |
| 提交前 | **汇总审核，且只拦一次** | 唯一需要强制拦截的节点 |

**例外**：`blocking: true` 的必填项在填充阶段即时提示，否则用户走到提交才发现要回头重填。
必填项识别属启发式的一部分：`required` 属性、标签中的 `*`、红色边框样式。

### 11.3 随时补充与回填闭环

- **增量录入**：密文库只加密新增记录（DEK 不变），不必全量重写
- **回填闭环**：用户在某次网申中手填了档案里没有的字段 → 询问「要保存到档案吗？」→ 确认后写回加密档案。
  **这是产品的核心价值闭环：填一次，以后自动。**
- **不静默保存**：避免污染密文库，也避免把偶发的 B 级信息悄悄留存
- **冲突处理**：新值与档案已有值不一致时展示两者让用户选择，不自动覆盖
- **schema 演进**：开放 `customFields` 槽位 + schema 版本号 + 迁移函数；否则用户加一个自定义字段就会撞破数据结构

### 11.4 提交前审核：护栏，而非功能

在提交按钮上拦截一次，展示摘要：

```
自动填充  12 项 · 全部来自档案
启发式推断  3 项 · 请重点核对      ← 高亮
仍为空的必填  2 项
本次新录入  1 项 · 待确认是否存档
```

**关键设计：把「自动填的」与「猜的」分开标注。** 用户只需重点核对启发式推断的部分，不必逐项复核全部 —— 比让他检查十几项高效得多。

**「本项目不代为提交」** —— 这既是代码行为，也是产品立场。ADR-6（不用无头浏览器）与「不做批量投递」的红线在此落地为一条具体规则：
**只拦截一次 → 展示摘要 → 放行由用户点击。**
拦截后不放行 = 阻碍；拦截后静默放行 = 欺骗。两者都不做。

### 11.5 验收清单（新增）

| 指标 | 阈值 | 性质 |
|---|---|---|
| 中英数字集合一致 | 100% | **一票否决** |
| 英文版无第一人称代词 / 无照片字段 | 100% | 硬约束 |
| 自我介绍字数落在目标档位 ±10% | 100% | 硬约束 |
| 英文口播版句均长度 | ≤ 25 词 | 优化目标 |

### 11.6 里程碑影响

原 M4（渲染导出）拆为：**M4a** 中文简历模板 + PDF 导出；**M4b** 英文简历规范集与跨语言一致性校验；**M4c** 中英自我介绍四档生成。
原 M5 增加：**提交前审核摘要**与**回填闭环**。
